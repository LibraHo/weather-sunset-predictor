# 需求文档

## 简介

天气晚霞预测器是一个Web应用程序，允许用户查询天气信息并预测火烧云（晚霞）出现的可能性和质量。系统主要通过 **Open-Meteo API** 获取气象数据（免费、无需密钥），分析多个气象参数（云层、湿度、能见度等），为用户提供晚霞观赏的最佳时机建议。

> ⚠️ **架构变更说明（2026-03）**：系统已从 Windy API 作为主数据源迁移至 Open-Meteo。Windy 现仅作为 emergency fallback 保留（默认关闭，需设置 `ENABLE_WINDY_EMERGENCY_FALLBACK=true`）。详见 `.kiro/windy-index.md`。

## 术语表

- **系统（System）**: 天气晚霞预测器Web应用程序
- **Windy_API**: Windy提供的Point Forecast API服务
- **火烧云（Sunset_Glow）**: 日落时分天空中出现的红色或橙色云彩现象
- **预测质量（Prediction_Quality）**: 火烧云出现可能性的评分，范围0-100
- **气象参数（Weather_Parameters）**: 包括温度、湿度、云层、风速、气压等数据
- **API_Key**: 用户的Windy API访问密钥

## 需求

### 需求 1：API密钥管理

**用户故事：** 作为用户，我想要安全地配置我的Windy API密钥，以便系统能够获取天气数据。

#### 验收标准

1. 当用户首次访问应用时，系统应显示API密钥配置界面
2. 当用户输入API密钥时，系统应将其存储在浏览器本地存储中
3. 当用户提交API密钥后，系统应验证密钥的有效性
4. 如果API密钥无效，则系统应显示错误消息并允许重新输入
5. 当API密钥已配置时，系统应允许用户查看和修改密钥

### 需求 2：位置选择

**用户故事：** 作为用户，我想要选择查询位置，以便获取特定地点的天气和晚霞预测。

#### 验收标准

1. 当用户访问主界面时，系统应提供位置输入功能
2. 当用户输入位置名称时，系统应将其转换为经纬度坐标
3. 当用户点击"使用当前位置"按钮时，系统应请求浏览器地理位置权限
4. 如果用户授予位置权限，则系统应获取当前GPS坐标
5. 当位置无法解析时，系统应显示友好的错误提示

### 需求 3：天气数据获取

**用户故事：** 作为用户，我想要查看当前和未来的天气信息，以便了解基本气象条件。

#### 验收标准

1. 当用户选择位置后，系统应调用Windy API获取天气数据
2. 当API请求发送时，系统应使用POST方法访问 https://api.windy.com/api/point-forecast/v2
3. 系统应请求以下气象参数：temp、rh、clouds、wind、pressure、visibility
4. 当API返回数据时，系统应解析并存储未来48小时的预测数据
5. 如果API请求失败，则系统应显示错误信息并允许重试

### 需求 4：基本天气显示

**用户故事：** 作为用户，我想要看到清晰的天气信息展示，以便快速了解当前气象状况。

#### 验收标准

1. 当天气数据加载完成时，系统应显示当前温度、湿度、风速和云量
2. 系统应以摄氏度显示温度值
3. 系统应以百分比显示湿度和云量
4. 系统应以公里/小时显示风速
5. 当显示天气信息时，系统应包含适当的图标和视觉元素

### 需求 5：火烧云预测算法

**用户故事：** 作为用户，我想要获得准确的火烧云预测，以便决定是否外出观赏朝霞和晚霞。

#### 验收标准

1. 当系统计算火烧云质量时，系统应分析中高层云量（30-70%为最佳）
2. 当系统计算火烧云质量时，系统应评估相对湿度（30-70%为最佳范围）
3. 当系统计算火烧云质量时，系统应考虑能见度因素（高能见度加分）
4. 当系统计算火烧云质量时，系统应检查低层云量（低层云少为佳）
5. 系统应输出0-100的预测质量评分
6. 当评分高于70时，系统应标记为"优秀"观赏条件
7. 当评分在40-70之间时，系统应标记为"良好"观赏条件
8. 当评分低于40时，系统应标记为"一般"观赏条件

### 需求 6：预测结果展示

**用户故事：** 作为用户，我想要看到直观的预测结果，以便快速理解朝霞和晚霞出现的可能性。

#### 验收标准

1. 当预测计算完成时，系统应显示预测质量评分
2. 系统应使用颜色编码表示预测质量（绿色=优秀，黄色=良好，灰色=一般）
3. 系统应显示影响预测的关键气象因素
4. 系统应显示最佳观赏时间（日落前后30分钟）
5. 当显示预测结果时，系统应提供详细的气象条件说明

### 需求 7：未来预测时间线

**用户故事：** 作为用户，我想要查看未来几天的晚霞预测，以便提前计划观赏活动。

#### 验收标准

1. 当天气数据可用时，系统应计算未来3天的晚霞预测
2. 系统应为每天的日落时间生成独立的预测评分
3. 系统应以时间线或列表形式展示多日预测
4. 未来预测卡片应保持简洁，仅显示分数、类型和时间，不展示可展开的详细气象参数
5. 系统应高亮显示预测质量最高的日期

### 需求 8：响应式设计

**用户故事：** 作为用户，我想要在不同设备上使用应用，以便随时随地查询晚霞预测。

#### 验收标准

1. 当用户在移动设备上访问时，系统应调整布局以适应小屏幕
2. 当用户在桌面设备上访问时，系统应充分利用大屏幕空间
3. 系统应在所有主流浏览器（Chrome、Firefox、Safari、Edge）上正常工作
4. 当屏幕方向改变时，系统应自动调整布局
5. 系统应确保所有交互元素在触摸屏上易于操作

### 需求 9：数据刷新

**用户故事：** 作为用户，我想要刷新天气数据，以便获取最新的预测信息。

#### 验收标准

1. 系统应提供手动刷新按钮
2. 当用户点击刷新按钮时，系统应重新请求Windy API数据
3. 当数据刷新时，系统应显示加载指示器
4. 系统应缓存数据30分钟以减少API调用
5. 如果缓存数据未过期，则系统应优先使用缓存数据

### 需求 10：错误处理

**用户故事：** 作为用户，我想要在出现问题时看到清晰的错误信息，以便知道如何解决问题。

#### 验收标准

1. 如果网络连接失败，则系统应显示"网络连接错误"消息
2. 如果API密钥无效，则系统应提示用户检查密钥配置
3. 如果API请求超时，则系统应显示超时错误并提供重试选项
4. 如果位置解析失败，则系统应建议用户尝试不同的位置名称
5. 当发生错误时，系统应保持应用的稳定性，不应崩溃

### 需求 11：天气界面优化

**用户故事：** 作为用户，我想要看到更全面的天气信息和直观的数据可视化，以便更好地了解天气变化趋势和做出决策。

#### 验收标准

1. 当用户查看天气面板时，系统应显示未来7天的每日天气概览
2. 当用户查看每日概览时，系统应显示日期、最高/最低温度、天气图标和降水概率
3. 当用户选择查看今天或明天的详细数据时，系统应显示24小时的小时级预报
4. 当显示小时级温度数据时，系统应使用折线图展示温度变化趋势
5. 当显示小时级降水数据时，系统应使用柱状图展示降水量或降水概率
6. 当显示小时级湿度数据时，系统应使用折线图或面积图展示湿度百分比变化
7. 当显示小时级风速数据时，系统应使用折线图展示风速变化，并用箭头图标显示风向
8. 当显示小时级气压数据时，系统应使用折线图展示气压变化趋势
9. 当显示小时级云量数据时，系统应使用面积图或柱状图展示云层覆盖百分比
10. 系统应支持用户在不同气象参数之间切换查看（温度、降水、湿度、风速、气压、云量）
11. 当显示图表时，系统应使用适当的颜色编码（如温度用蓝→橙→红渐变，降水用蓝色系）
12. 系统应在图表上标注关键数值点，便于用户快速读取数据
13. 当用户在移动设备上查看时，图表应支持横向滚动以显示完整的24小时数据


### 需求 12：朝霞晚霞预测增强功能

**用户故事：** 作为摄影爱好者或观赏者，我想要获得更专业的朝霞和晚霞预测信息，以便更好地规划拍摄或观赏活动。

#### 验收标准

1. 当系统显示预测时，系统应同时显示日出和日落的精确时间
2. 当系统显示预测时，系统应标注"黄金时段"（Golden Hour）的时间范围（日出后/日落前30-60分钟）
3. 当系统显示预测时，系统应标注"蓝调时段"（Blue Hour）的时间范围（日出前/日落后20-30分钟）
4. 当系统显示预测时，系统应分别计算和显示朝霞（日出）和晚霞（日落）的独立评分
5. 当预测质量较高时（评分>70），系统应显示太阳方位角信息，帮助用户确定最佳拍摄/观赏方向
6. 系统应提供预测提醒功能，允许用户设置在高质量预测时接收浏览器通知
7. 当用户启用提醒时，系统应允许用户自定义提醒阈值（如评分>70时提醒）
8. 当用户启用提醒时，系统应在符合条件时发送浏览器通知，包含日期、时间和评分信息
9. 系统应支持用户收藏多个位置，并在位置列表中快速切换
10. 当用户收藏位置时，系统应保存位置名称和坐标到本地存储
11. 当用户查看预测时，系统应显示云层分层信息，包括高云（>6km）、中云（2-6km）、低云（<2km）的覆盖百分比
12. 当显示云层信息时，系统应用不同颜色或图标区分不同高度的云层
13. 当显示预测详情时，系统应说明各层云对朝霞/晚霞效果的影响（如"中高云适中，有利于火烧云形成"）


### 需求 13：最近搜索历史

**用户故事：** 作为用户，我想要快速访问最近搜索过的城市，以便无需重复输入常用位置。

#### 验收标准

1. 当用户成功查询某个位置的天气后，系统应将该位置保存到搜索历史中
2. 系统应在本地存储中保存最近搜索的5个城市（包括城市名称和坐标）
3. 当搜索历史超过5个时，系统应自动删除最早的记录，保持最多5个
4. 当用户点击位置输入框时，系统应显示最近搜索历史的下拉列表
5. 当用户点击历史记录中的某个城市时，系统应自动填充该城市并加载天气数据
6. 当显示搜索历史时，系统应按时间倒序排列（最新的在最上面）
7. 系统应提供清除单个历史记录的功能（如每个记录旁边的删除按钮）
8. 系统应提供清除全部历史记录的功能
9. 当用户搜索已存在于历史中的城市时，系统应将该城市移到历史列表的最前面，而不是创建重复记录
10. 当历史记录为空时，系统应显示提示信息（如"暂无搜索历史"）


### 需求 14：多语言支持

**用户故事：** 作为用户，我想要使用母语或其他熟悉语言来浏览应用，以便更好地理解和使用应用功能。

#### 验收标准

1. 当用户访问应用时，系统应自动检测浏览器的语言设置
2. 当检测到的语言在支持列表中时，系统应自动切换到该语言
3. 当检测到的语言不在支持列表中时，系统应使用默认语言（简体中文）
4. 系统应提供语言切换功能，允许用户手动选择界面语言
5. 系统应在设置或导航栏中提供语言选择器（下拉菜单或按钮）
6. 当用户切换语言时，系统应保存语言偏好到本地存储
7. 当用户切换语言后，系统应刷新界面并应用新语言
8. 系统应支持至少10种语言：简体中文（zh-CN）、繁体中文（zh-TW）、英语（en-US）、日语（ja-JP）、韩语（ko-KR）、越南语（vi-VN）、法语（fr-FR）、西班牙语（es-ES）、意大利语（it-IT）、阿拉伯语（ar-SA）
9. 当用户使用阿拉伯语时，系统应正确处理RTL（Right-to-Left）布局
10. 系统应为RTL语言镜像必要的UI元素（如图标、边距、对齐方式）
11. 当显示日期时，系统应根据语言自动格式化（如"2026年1月25日" vs "January 25, 2026"）
12. 当显示数字时，系统应根据语言自动格式化千分位和小数点（如"1,234.56" vs "1.234,56"）
13. 当显示时间时，系统应根据语言使用12小时制或24小时制（英语国家倾向12小时制）
14. 当显示百分比时，系统应根据语言格式化（如"85%"在不同语言中格式一致）
15. 系统应提供语言切换确认机制，防止用户误操作切换语言
16. 当切换语言时，系统应保留当前的用户数据（如位置、API密钥、收藏列表）
17. 系统应在所有界面文本中支持翻译（包括标题、按钮、提示信息、错误消息）
18. 当翻译文本缺失时，系统应回退到默认语言的对应文本


### 需求 15：后端代理 Windy API 与密钥保护

**用户故事：**  
作为系统所有者，我希望 Windy API 密钥只保存在后端，由后端统一代理调用 Windy API，这样可以避免在前端代码或网络请求中暴露密钥，降低被滥用和泄露的风险。

#### 验收标准

1. 系统应提供一个后端天气数据接口（例如 `/api/weather/forecast`），用于替代前端直接访问 `https://api.windy.com/api/point-forecast/v2`。  
2. 后端应从环境变量或安全配置文件中读取 Windy API 密钥（如 `WINDY_API_KEY`），前端和代码仓库中不得出现真实密钥。  
3. 当前端请求后端天气接口时，后端应使用配置中的密钥向 Windy API 发起请求，并将 Windy 返回的数据解析为系统所需格式再返回给前端。  
4. 浏览器开发者工具中（包括页面源代码和 Network 请求信息），不应出现 Windy API 密钥或可推断出该密钥的敏感字段。  
5. 当后端调用 Windy API 失败（如网络错误、超时、配额限制、无效坐标等）时，后端应返回明确的错误状态和人类可读的错误信息，前端应展示友好的错误提示并允许重试。  
6. 后端接口应支持基础的访问日志记录，以便后续排查问题和监控调用情况（例如记录请求时间、坐标参数、Windy 响应状态等，不记录敏感密钥本身）。


### 需求 16：统一设置面板

**用户故事：**  
作为用户，我希望有一个统一的设置面板，可以在一个地方集中管理与应用相关的配置（如 API 使用方式、通知、语言等），以便更方便地自定义应用行为，而不需要在多个位置查找和修改设置。

#### 验收标准

1. 系统应在主界面提供明显可见的「设置」入口（例如右上角齿轮按钮），点击后打开设置面板或模态框。  
2. 设置面板至少应包含以下分组区域：  
   - 「数据源与网络」：展示当前使用的 API 模式（真实 Windy 后端代理 / 模拟数据），并提供只读说明文案（不在前端填写密钥）。  
   - 「通知与提醒」：集中管理晚霞预测通知的开关、评分阈值等（可复用现有通知设置功能的 UI/逻辑）。  
   - 「语言与显示」：展示当前语言选择入口（可复用现有语言选择器），并提供与界面相关的基础说明。  
3. 当用户在设置面板中修改通知开关或阈值时，系统应立即保存并生效，且与现有通知功能使用相同的数据存储逻辑（如 `StorageService` 中的通知设置）。  
4. 当用户在设置面板中切换语言时，系统应立即更新界面文案，与现有多语言机制保持一致（包括标题、按钮、提示信息等）。  
5. 设置面板在桌面端和移动端都应具备良好的可用性：  
   - 在小屏幕上应自适应布局，无内容被裁剪或难以点击的情况；  
   - 关闭按钮或返回操作明显可见，可通过点击遮罩或显式按钮关闭。  
6. 设置面板的所有文案应接入多语言系统，至少覆盖当前支持的语言（如 zh-CN、en-US 等），避免出现硬编码的单一语言文本。  
7. 若后续引入更多可配置项（例如主题、单位制等），应能够在不破坏现有交互的前提下扩展到设置面板中。


### 需求 17：个性化设置（单位、主题、默认位置）

**用户故事：**  
作为经常使用本应用的用户，我希望能够自定义温度/风速等显示单位、界面主题（暗色/自适应）以及默认位置，这样每次打开应用时都能看到符合自己习惯的界面和数据。

#### 验收标准

1. 系统应允许用户在设置面板中选择 **温度单位**（至少支持 ℃ / ℉），并在所有相关界面中按所选单位显示温度值（包括当前天气、未来预报、图表等）。  
2. 系统应允许用户在设置面板中选择 **风速单位**（如 m/s、km/h，若已有单位规范可按现有约定扩展），并在所有显示风速的地方统一采用用户选择的单位。  
3. 当用户修改单位设置后，界面上已有的数据展示应在不刷新页面的前提下更新为新单位，避免需手动重新搜索。  
4. 系统应提供 **主题设置** 选项，至少包括：  
   - 明亮模式（Light）  
   - 暗色模式（Dark）  
   - 自适应系统（Follow System，优先跟随操作系统 / 浏览器的深浅色偏好）  
5. 当用户切换主题时，界面应立即应用新主题（包括背景、文字、卡片、图表配色等），并在后续访问时保持用户上次选择的主题。  
6. 若选择「自适应系统」主题，当系统/浏览器深浅色偏好发生变化时（如夜间模式开启），应用应在用户下一次打开或刷新页面时自动调整主题。  
7. 系统应在设置面板中提供 **默认位置** 设置：  
   - 用户可以在已成功查询的位置中选择一个作为默认位置；  
   - 当用户下次打开应用时，如果已设置默认位置且未显式选择“使用当前位置”，系统应自动加载该默认位置的天气和晚霞预测。  
8. 默认位置的保存和读取应复用现有的本地存储机制（如 `StorageService`），并与搜索历史/收藏位置逻辑兼容（不会产生重复或冲突）。  
9. 所有个性化设置（单位、主题、默认位置）在浏览器关闭后再次打开时仍然有效，除非用户主动重置或清除。  
10. 设置相关的所有文案和选项名称必须接入多语言系统，在已支持语言下显示本地化文本。


### 需求 18：集成 Windy Map Forecast 地图预测 API

**用户故事：**  
作为用户，我希望在应用中直接看到基于 Windy 官方地图的动态天气可视化（风、云、降水等图层），并与当前地点和晚霞预测结果联动展示，这样可以更直观地理解天气形势和火烧云形成条件。

#### 参考文档

- [Windy Map Forecast API 文档](https://api.windy.com/map-forecast/docs)

#### 验收标准

1. 系统应在界面中提供一个「地图预测」区域或独立视图，使用 Windy Map Forecast API 在页面上渲染交互式天气地图（基于 Leaflet + `windyAPI` 对象）。  
2. 当用户选择或更新查询位置（经纬度）时，地图应自动平移/缩放到该位置附近，并显示对应的天气图层（例如风、温度、云量等）。  
3. 地图视图应至少支持以下基本交互能力：  
   - 拖动、缩放地图；  
   - 通过时间轴或进度控制查看未来一段时间的天气演变（如果使用 Windy 提供的时间控制能力）；  
   - 切换至少 1–2 个主要覆盖图层（如风场、降水或云量），具体图层可根据 Map Forecast API 能力选择。  
4. 地图所使用的图层、单位和配色应与 Windy Map Forecast API 的默认行为保持一致，或基于官方文档中 `store`、`overlays`、`colors` 等接口进行配置，确保效果稳定且不依赖未文档化的内部接口。  
5. 应用在加载 Windy 地图时，必须遵守 Windy API 的使用条款和环境区分：  
   - 测试环境只能使用 Testing 版本，不得在生产中使用；  
   - 上线到生产时，应根据 Windy 的定价与许可要求使用 Professional 版本。  
6. Windy Map Forecast API 的密钥管理方式应与整体 Windy API 策略一致，不在前端源码或公开仓库中明文暴露真实密钥，具体方案需符合 Windy 官方关于 Map Forecast API 的集成与安全建议。  
7. 当 Windy Map Forecast 地图加载失败（例如网络错误、配额超限或密钥配置错误）时，系统应在地图区域展示清晰的错误提示和必要的操作建议（如「稍后重试」或「联系管理员检查 API 配置」），同时不影响其他核心功能的正常使用。


### 需求 19：周边火烧云可视化

**用户故事：** 作为摄影爱好者或观赏者，我想要查看周边不同方位的火烧云预测分布图，以便选择最佳的拍摄或观赏地点。

#### 验收标准

1. 当系统生成朝霞或晚霞预测时，系统应分析周边区域（以用户位置为中心，半径50-100公里）的火烧云可能性
2. 当系统分析周边区域时，系统应在不同方位（北、东北、东、东南、南、西南、西、西北）采样气象数据
3. 当系统获取周边气象数据时，系统应计算每个方位的火烧云评分（0-100）
4. 当显示周边火烧云图时，系统应使用雷达图（Radar Chart）或极坐标图展示8个方位的评分分布
5. 当周边火烧云图渲染时，系统应使用渐变颜色表示评分强度（绿色=优秀，黄色=良好，灰色=一般）
6. 当用户点击某个方位时，系统应显示该方向的详细气象信息和观赏建议
7. 当周边火烧云图显示时，系统应标注最佳观赏方位（评分最高的方向）
8. 当周边火烧云图显示时，系统应提供距离信息（每个采样点距离用户位置的公里数）
9. 当系统计算周边火烧云评分时，系统应考虑该方向的云层分布、湿度和能见度
10. 当周边火烧云预测完成后，系统应生成"观赏方向建议"文本（如"建议向东南方向观赏，该方向火烧云质量最高"）
11. 当用户在移动设备上查看时，周边火烧云图应支持触摸交互（旋转、缩放）
12. 当周边火烧云图渲染失败时，系统应显示降级UI（方位列表或表格）
13. 当用户切换位置时，系统应重新计算并更新周边火烧云图
14. 当显示周边火烧云图时，系统应提供图例说明（评分范围、颜色含义）
15. 系统应支持用户自定义采样半径（50公里、100公里、150公里）


### 需求 20：火烧云地图覆盖层

**用户故事：** 作为用户，我想要在地图上直接看到火烧云预测的地理分布热力图，以便直观了解区域内的火烧云观赏潜力分布。

#### 参考文档

- NOAA GFS 数据: https://www.ncei.noaa.gov/products/weather-climate-models/global-forecast-system
- GRIB2 数据格式: https://www.nco.ncep.noaa.gov/pmb/docs/grib2/grib2_doc/

#### 验收标准

1. 当用户查看地图预测时，系统应提供"火烧云覆盖层"选项
2. 当用户启用火烧云覆盖层时，系统应获取周边区域（约200-300公里范围）的GFS气象数据
3. 当系统获取GFS数据时，系统应下载以下变量：
   - TCDC (Total Cloud Cover) - 总云量
   - LCDC (Low Cloud Cover) - 低层云量（<2km）
   - MCDC (Middle Cloud Cover) - 中层云量（2-6km）
   - HCDC (High Cloud Cover) - 高层云量（>6km）
4. 当系统处理GFS数据时，系统应对每个像素点应用"光路追踪+云量评分"算法：
   - 向西（日落方向）检查邻近像素的LCDC值以评估光路阻隔
   - 结合本地的MCDC/HCDC计算火烧云概率值（0.0-1.0）
5. 当系统生成覆盖层图像时，系统应创建带透明通道的PNG图片：
   - 概率0 = 完全透明
   - 概率100 = 红色/橙色渐变，不透明度60%
   - 图像分辨率适配地图缩放级别（建议512x512或1024x1024）
6. 当覆盖层生成完成后，系统应记录图像的四个角坐标（Bounds）以便准确贴图
7. 当系统显示覆盖层时，系统应在Windy地图上叠加PNG图像作为自定义图层
8. 当覆盖层显示时，系统应提供图例说明（颜色含义：红橙色=高概率，黄色=中等概率，灰色=低概率）
9. 当用户调整地图位置或缩放时，系统应重新获取对应区域的GFS数据并更新覆盖层
10. 当GFS数据获取失败时，系统应显示错误提示并回退到雷达图模式
11. 系统应缓存生成的覆盖层图像（有效期30分钟）以减少重复计算
12. 当用户在移动设备上查看时，覆盖层应支持触摸缩放和平移
13. 系统应提供覆盖层刷新按钮，允许用户手动更新数据
14. 当覆盖层加载时，系统应显示加载动画或进度指示器
15. 系统应支持切换朝霞和晚霞的覆盖层显示


**✅ 实现状态（2026-02-06更新）**：

**状态**: **已完成 - Phase 6 Leaflet 重构**

**已完成**:
- ✅ 前端 WindyMapService 重构（Leaflet + OpenStreetMap 替代 iframe）
- ✅ 前端 FireCloudOverlayService 重构（L.imageOverlay() 替代 DOM 覆盖层）
- ✅ 后端 FireCloudService（Python GFS 处理器封装、缓存、错误处理）
- ✅ 后端 API 路由（/api/firecloud/overlay, /health, /cache/clear）
- ✅ Python GFS 处理器（769行，光路追踪 + 高斯评分算法，19 个测试）
- ✅ 前后端集成测试（19 个新测试，216 个后端测试全部通过）
- ✅ 覆盖层与地图完美同步（Leaflet 原生支持拖动/缩放同步）

**架构改进**:
- 使用 Leaflet + OSM 替代 iframe 嵌入方式（方案C - 混合方案）
- 覆盖层通过 `L.imageOverlay()` 直接添加到 Leaflet 地图层
- 支持后端 GFS 数据 + 前端 Canvas 双数据源，自动回退
- 30 分钟缓存策略，60 秒 Python 超时保护

**当前UI状态**: 地图覆盖层开关已隐藏，用户无法访问此功能


### 需求 21：UI毛玻璃效果

**用户故事：** 作为用户，我希望应用界面具有现代化的毛玻璃（Glassmorphism）视觉效果，让卡片、面板和导航栏呈现半透明磨砂质感，提升整体视觉层次感和美观度。

#### 验收标准

1. 当用户查看应用界面时，主要卡片组件（`.card`）应具有毛玻璃效果：半透明背景、背景模糊（`backdrop-filter: blur()`）和微妙的边框
2. 当用户查看顶部导航栏（`header`）时，导航栏应具有毛玻璃效果，在页面滚动时能透视下方内容
3. 当用户查看晚霞预测卡片（`.prediction-card`）时，卡片应具有毛玻璃效果，与普通卡片保持视觉一致性
4. 当用户打开模态框（`.modal-content`、设置面板）时，模态框内容区域应具有毛玻璃效果
5. 当用户悬停在毛玻璃卡片上时，系统应提供适当的悬停反馈效果（如增强模糊度或调整透明度）
6. 系统应在明亮模式和暗色模式下分别提供适配的毛玻璃效果参数（明亮模式使用浅色半透明背景，暗色模式使用深色半透明背景）
7. 系统应通过CSS自定义属性（CSS Variables）定义毛玻璃效果参数，便于统一调整和主题切换
8. 当浏览器不支持 `backdrop-filter` 属性时，系统应优雅降级为不透明背景，确保内容可读性不受影响
9. 毛玻璃效果不应明显影响页面渲染性能，在移动设备上应保持流畅的滚动和交互体验
10. 当用户在移动设备上使用时，毛玻璃效果应适当降低模糊强度或使用更简化的效果，以优化性能
11. 系统应确保毛玻璃效果下的所有文字内容保持足够的对比度和可读性（符合WCAG AA标准）
12. 底部页脚（`footer`）应具有与导航栏风格一致的毛玻璃效果

---

### 需求 22：前后端分离架构重构

**用户故事：** 作为开发者/系统维护者，我希望将核心预测算法和业务逻辑迁移到后端，实现真正的前后端分离架构，以便：
- 支持多平台客户端（Web、iOS、Android、小程序）复用同一套后端 API
- 减少前端代码体积和浏览器 CPU 占用
- 支持后端缓存和算法 A/B 测试
- 提高系统可维护性和可扩展性

#### 验收标准

**Phase 1：核心预测算法后端化**

1. 后端应提供预测计算 API `POST /api/prediction/calculate`，接收天气数据、日期、坐标、预测类型，返回预测结果 JSON
2. 预测结果应包含：评分（score）、质量等级（quality）、各因素得分（factors）、日出/日落时间、黄金时段、蓝调时段
3. 后端预测算法应与当前前端算法（`SunsetPredictionService`）计算结果保持一致
4. 前端应改为调用后端 API 获取预测结果，不再在浏览器端执行预测算法
5. 系统应提供算法版本标识，支持未来算法迭代和 A/B 测试

**Phase 2：周边采样聚合**

6. 后端应提供周边预测聚合 API `POST /api/prediction/surrounding`，一次请求返回 8 个方向的预测结果
7. 后端应在服务端并行获取 8 个方向点的天气数据，减少网络往返次数（从 8×RTT 降至 1×RTT）
8. 前端周边火烧云功能应改为调用聚合 API，不再发起多个并行请求
9. 后端应支持周边数据缓存，相同位置/半径/时间的请求可复用缓存

**Phase 3：增强预测模型后端化**

10. 后端应提供增强预测 API `POST /api/prediction/enhanced`，包含画布评分、光路通透评分等高级功能
11. 后端应支持预测模型选择参数，允许客户端指定使用基础或增强模型
12. 增强预测 API 应保持与当前前端 `EnhancedSunsetPredictionService` 计算结果一致

**API 设计规范**

13. 所有预测 API 应返回统一的响应格式：`{ success: boolean, data: object, error?: { code, message } }`
14. API 应支持请求参数验证，对无效参数返回 400 错误码和描述性错误信息
15. API 应记录请求日志，包含请求参数、响应时间、错误信息（如有）

**性能要求**

16. 单点预测 API 响应时间应 < 500ms（不含天气数据获取时间）
17. 周边聚合 API 响应时间应 < 2000ms（包含 8 个点的天气数据获取）
18. 后端预测计算应比前端快（得益于 Node.js 优化）

**兼容性**

19. 前端应保留对旧 API 的兼容，支持渐进式迁移
20. 系统应提供 API 版本管理机制，便于未来 API 升级


### 需求 23：测试覆盖率达标

**用户故事：** 作为开发者，我希望代码库的测试覆盖率达到并持续维持规定阈值，以便保障代码质量、防止回归缺陷，并为后续重构和功能扩展提供安全网。

#### 验收标准

**覆盖率阈值（Jest 全局配置）**

1. 系统的语句覆盖率（Statements）应达到 **≥ 80%**
2. 系统的分支覆盖率（Branches）应达到 **≥ 75%**
3. 系统的函数覆盖率（Functions）应达到 **≥ 90%**
4. 系统的行覆盖率（Lines）应达到 **≥ 80%**
5. 当运行 `npm run test:coverage` 时，所有阈值均通过，无覆盖率失败报告

**覆盖率范围界定**

6. 以下文件应从覆盖率统计中排除，因为它们是纯数据/测试替身，不包含可测业务逻辑：
   - `src/locales/*.js`（语言翻译数据文件）
   - `src/services/Mock*.js`（离线开发用的模拟服务）
   - `tests/**`（测试文件本身）
7. 以下文件属于生产代码，应纳入覆盖率统计并补充测试：
   - `src/utils/UnitConverter.js`（单位转换工具）
   - `src/services/ConfigService.js`（配置服务）
   - `src/services/ThemeService.js`（主题管理服务）
   - `src/services/StorageService.js`（本地存储服务）
   - `src/services/NotificationService.js`（通知服务）
   - `src/services/SurroundingPointsService.js`（周边采样服务）
   - `src/services/RadarChartService.js`（雷达图服务）
   - `src/services/FireCloudOverlayService.js`（地图覆盖层服务）
   - `src/services/WindyMapService.js`（地图服务）
   - `src/components/SettingsPanel.js`（设置面板组件）
   - `src/components/LanguageSelector.js`（语言选择组件）

**各模块覆盖率子目标**

8. `src/utils/` 目录整体函数覆盖率应达到 **≥ 90%**
9. `src/models/` 目录整体函数覆盖率应达到 **≥ 90%**
10. `src/services/` 目录整体语句覆盖率应达到 **≥ 75%**
11. `src/controllers/` 目录整体语句覆盖率应达到 **≥ 60%**（受限于 DOM 环境复杂性）

**测试质量要求**

12. 新增测试应遵循现有 Jest + jsdom 框架，不引入新的测试框架依赖
13. Canvas API、Leaflet、浏览器通知等原生 API 应使用 Jest mock 进行模拟，不跳过相关代码路径
14. 每个新增测试文件应包含正常路径（happy path）和异常路径（error path）的测试用例
15. 新增测试不应破坏现有 667 个通过的测试用例


## 变更记录（2026-02-13）

### 配置策略调整：移除前端直连模式

- 前端必须固定使用后端代理访问 API，不再提供“直连模式”切换。
- 前端设置菜单移除 API 模式选择项，仅保留后端服务器地址（Proxy URL）配置入口。
- API Key 管理入口迁移至后端（环境变量与后端配置），前端不再作为必填前置条件。

#### 验收标准（新增）

1. 设置菜单不再出现“API 访问模式”下拉选择器。
2. 前端初始化时即使本地没有 API Key，也可正常进入应用。
3. 前端所有天气/预测请求均经由后端代理发起。
4. 前端仍可配置后端地址（如 `api_proxy_url`）用于开发环境切换。


---

## 变更记录（2026-02-14）

### 需求 24：中国国内可用的定位服务

**用户故事：** 作为用户（尤其是中国大陆用户），我想要一个在中国境内可正常使用的位置搜索服务，并能在设置界面自由选择定位服务的「调用模式」和「提供商」。

#### 背景说明

- **当前实现**（`GeocodingService.js`）：前端**直连** OpenStreetMap Nominatim，在中国大陆可能因 GFW 导致访问缓慢或失败。这不是 Google，是免费的 OSM 服务。
- **Google Maps**：覆盖最全，但需付费 API Key，中国大陆直连不可用。
- **高德地图**：中国大陆最优选，免费 API Key，地名覆盖完整，仅限后端代理模式（防止 Key 暴露在前端）。
- 新方案采用**二层架构**：用户先选「模式」，再选「提供商」。

#### 二层架构设计

```
调用模式 (geocoding_mode)
├── 后端代理 backend（推荐）— 前端请求 /api/geocoding/*，后端转发
│   ├── Nominatim/OSM（默认）— 免费，后端可访问 OSM
│   ├── 高德地图            — 中国首选，需用户填入免费 Key
│   └── Google Maps        — 需付费 Key，后端部署在境外时可用
└── 前端直连 direct         — 前端直接请求外部 API
    ├── Nominatim/OSM       — 免费，中国直连可能受限
    └── Google Maps         — 需付费 Key，中国不可用
```

> 注：高德地图仅支持「后端代理」模式（避免 API Key 暴露在浏览器）

#### 验收标准

1. 后端新增 `/api/geocoding/search` 和 `/api/geocoding/reverse` 端点，支持 `provider=nominatim|gaode|google` 参数
2. 前端新增 `BackendGeocodingService`，通过后端代理发起地理编码请求
3. 前端新增 `GeocodingServiceFactory`，读取 `geocoding_mode` + `geocoding_provider` 返回对应服务实例
4. 设置面板「数据源」区新增「位置解析服务」二级配置：
   - **模式选择**（单选）：后端代理 / 前端直连
   - **提供商选择**（根据模式动态显示）：
     - 后端代理时：Nominatim / 高德地图 / Google Maps
     - 前端直连时：Nominatim / Google Maps
5. 选择高德地图或 Google Maps 时，显示对应 API Key 输入框；Key 存入 `localStorage.geocoding_api_key`
6. 切换设置后立即生效，下次搜索使用新配置，无需刷新页面
7. 高德/Google Key 未填写时，搜索报错提示「请先填写 API Key」
8. 各选项展示「中国可用」标记，帮助用户快速识别

#### 新增 API 端点

| 端点 | 方法 | 参数 | 说明 |
|------|------|------|------|
| `/api/geocoding/search` | GET | `q`, `provider`, `key` | 正向地理编码，provider: nominatim\|gaode\|google |
| `/api/geocoding/reverse` | GET | `lat`, `lon`, `provider`, `key` | 反向地理编码 |

#### 配置 localStorage 键

| 键 | 值 | 说明 |
|---|---|---|
| `geocoding_mode` | `'backend'`（默认）\| `'direct'` | 调用模式 |
| `geocoding_provider` | `'nominatim'`（默认）\| `'gaode'` \| `'google'` | 提供商 |
| `geocoding_api_key` | 字符串 | 高德或 Google API Key |

#### 工作量评估

| 任务 | 文件 | 估算行数 |
|------|------|---------|
| 后端路由 `server/routes/geocoding.js`（含 Google） | ✅ 已完成 | ~250 行 |
| 注册路由 `server/index.js` | ✅ 已完成 | +2 行 |
| `src/services/BackendGeocodingService.js` | ✅ 已完成 | ~150 行 |
| `src/services/GeocodingServiceFactory.js`（二层架构） | ✅ 已完成 | ~150 行 |
| `src/components/SettingsPanel.js` — 模式+提供商 UI | ⏳ 待实现 | +100 行 |
| `src/app.js` — 使用 Factory | ⏳ 待实现 | +5 行 |
| i18n `zh-CN.js` + `en-US.js` | ⏳ 待实现 | +30 行/文件 |
| 单元测试 `BackendGeocodingService` | ⏳ 待实现 | ~120 行 |
| 单元测试 `GeocodingServiceFactory` | ⏳ 待实现 | ~80 行 |
| 后端路由测试 `geocoding.test.js` | ⏳ 待实现 | ~130 行 |
| **合计** | | **~1050 行** |

---

### 需求 25：用户可配置 Windy API Key

**用户故事：** 作为用户，我可以在设置界面选择使用「系统提供的 Windy API」（由后端统一管理），或者输入**我自己的 Windy API Key**，以支持个人额度管理和多用户场景。

#### 背景说明

- 当前 Windy API Key 固定存储于后端 `.env`，所有用户共享同一个 Key 额度。
- 未来支持多用户或高频用户自带 Key，可大幅缓解共享 Key 的速率限制问题。
- 前端将 Key 存入 `localStorage`，每次请求通过自定义 HTTP 头 `X-Windy-API-Key` 传递给后端，后端优先使用该 Key 调用 Windy API。

#### 验收标准

1. 设置面板「数据源」区新增「Windy API 来源」配置：
   - **使用系统 API（推荐）** — 不填写 Key，使用后端 `.env` 中的 Key
   - **使用我的 API Key** — 用户输入自己的 Windy Point Forecast API Key
2. 当选择「使用我的 API Key」时，显示 Key 输入框（`password` 类型），Key 存入 `localStorage.user_windy_api_key`
3. 前端 `WindyAPIService.fetchFromProxy()` 在请求后端时，若 `localStorage.user_windy_api_key` 有值则添加 `X-Windy-API-Key` 请求头
4. 后端 `server/routes/weather.js` 读取 `X-Windy-API-Key` 头，非空时传递给 `windyService.fetchWeatherData()`
5. 后端 `windyService.fetchWeatherData()` 接受可选 `userApiKey` 参数，优先于 `process.env.WINDY_API_KEY`
6. 用户 Key 在传输中仅存在于 HTTP 请求头，不写入服务端日志
7. Key 格式简单校验（非空、长度 > 8 字符），不合法时前端提示并不保存

#### 工作量评估

| 任务 | 文件 | 估算行数 |
|------|------|---------|
| 后端 `server/routes/weather.js` — 读取用户 Key 头 | 修改 | +5 行 |
| 后端 `server/services/windyService.js` — 接受 `userApiKey` 参数 | 修改 | +8 行 |
| 前端 `src/services/WindyAPIService.js` — 发送 Key 头 | 修改 | +10 行 |
| `src/components/SettingsPanel.js` — 新增 Windy Key UI 段 | 修改 | +60 行 |
| i18n `zh-CN.js` + `en-US.js` | 修改 | +15 行/文件 |
| 单元测试（windyService + WindyAPIService） | 修改/新增 | +80 行 |
| **合计** | | **~200 行** |

---

### 两项需求总工作量汇总

| 需求 | 预估行数变更 | 新增文件数 | 修改文件数 |
|------|------------|-----------|-----------|
| 需求 24：中国定位服务 | ~850 行 | 4 | 5 |
| 需求 25：用户 Windy API Key | ~200 行 | 0 | 4 |
| **合计** | **~1050 行** | **4** | **7** |

> **注**：测试覆盖率需维持现有阈值（≥75% 分支 / ≥85% 函数），新增功能必须附带对应单元测试。

---

### 需求 26：主页分页菜单与火烧云算法说明页面

**用户故事：** 作为首次使用的用户，我希望主页菜单支持分页，并能查看一页专门介绍火烧云计算方法的内容，以便快速理解产品功能和预测依据。

#### 验收标准

1. 主页菜单应新增分页机制，至少包含「预测功能页」和「火烧云计算方法页」两个分页入口
2. 当用户点击分页菜单时，系统应在不刷新整页的情况下切换对应内容区域
3. 当前激活分页应有明显视觉状态（如高亮、下划线或背景区别）
4. 新增「火烧云计算方法」页面应说明核心评分因素：中高云量、低云遮挡、湿度、能见度
5. 新页面应包含评分区间说明（优秀/良好/一般）与示例解释，帮助用户理解分数含义
6. 新页面文案应纳入多语言体系（至少补齐 `zh-CN` 与 `en-US`）
7. 在移动端与桌面端中，分页菜单和新页面内容均应保持可读与可操作
8. 该需求本次仅更新规格文档（requirements/design/tasks），不要求立即进入开发实现

#### 交互与内容边界

- 分页菜单属于主页信息架构调整，不改变现有 API 接口
- 「火烧云计算方法」页面优先采用静态说明 + 图示占位，后续可扩展为交互式演示
- 若页面渲染异常，系统应回退到默认「预测功能页」并保留错误提示能力


---

### 需求 27：品牌升级 — 霞客 / Sunset Voyager Logo

**用户故事：** 作为产品负责人，我希望网站顶栏使用统一的品牌 Logo，替代纯文字标题，提升产品视觉识别度。

#### 验收标准

1. 顶栏标题替换为 SVG 内联 Logo（图标 + 文字横排）
2. Logo 图标为日落水平线造型，使用 `currentColor` 支持深色/浅色主题
3. 文字根据当前语言显示：
   - 中文（zh-CN / zh-TW）：霞客
   - 日文（ja-JP）：霞客
   - 韩文（ko-KR）：하객(霞客)
   - 其他语言：Sunset Voyager
4. 字体使用 Cormorant Garamond（衬线，优雅感），回退 Georgia
5. 桌面端（≥768px）图标 48px、字号 2rem；移动端图标 36px、字号 1.5rem
6. Logo 颜色统一为 `#C49A3C`（暖金色）

---

### 需求 28：设置界面重组

**用户故事：** 作为用户，我希望设置界面布局清晰、分区合理，便于快速找到所需配置项。

#### 验收标准

1. 设置分为以下独立 Section，每个 Section 有明确标题：
   - 🌐 语言与显示（语言、主题、温度单位、风速单位）
   - ⭐ 默认位置（收藏列表 + 设为默认）
   - 📍 位置解析（提供商选择）
   - 🔑 Windy API（系统/自定义模式）
   - 🔔 通知（开关 + 阈值）
   - ⚙️ 高级（代理 URL，折叠默认）
2. 位置解析提供商选项：
   - 高德地图（后端代理，默认，Key 存服务器 .env）
   - Nominatim（后端代理）
   - Nominatim（前端直连，适合本地开发）
3. 位置解析 API Key 由服务器端 `.env` 管理，前端不显示 Key 输入框

---

### 需求 29：访客计数持久化

**用户故事：** 作为运营者，我希望访客计数在服务重启、代码更新后不丢失。

#### 验收标准

1. 访客计数使用 SQLite 持久化存储，数据库路径为 `~/.xiake/visitor.db`
2. 数据库与代码目录完全隔离，代码更新/重部署不影响数据
3. SQLite 不可用时自动降级为内存计数，服务不崩溃
4. GET `/api/visitor/count` 返回当前计数
5. POST `/api/visitor/count` 原子递增并返回最新值


---

### 需求 30：日落投稿社区功能（规划中）

**用户故事：** 作为霞客用户，我希望能上传自己拍摄的日落/火烧云照片，与社区分享，并查看其他用户在同一地点的拍摄记录。

#### 核心功能（待细化）

1. **照片投稿**
   - 用户上传日落照片，关联拍摄时间和地理位置
   - 支持简短描述/标签

2. **社区浏览**
   - 按地点、日期、评分筛选浏览投稿
   - 地图叠加层展示投稿热点

3. **互动**
   - 点赞 / 收藏
   - 评论（可选）

#### 数据库设计方向

沿用 SQLite（`~/.xiake/xiake.db`），统一管理所有持久化数据：

```sql
-- 投稿表
CREATE TABLE submissions (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id     TEXT,                    -- 匿名 UUID 或未来账户 ID
  lat         REAL NOT NULL,
  lon         REAL NOT NULL,
  location    TEXT,                    -- 地名
  photo_path  TEXT NOT NULL,           -- 图片存储路径
  description TEXT,
  score       INTEGER,                 -- 当天预测评分（可选）
  taken_at    DATETIME NOT NULL,       -- 拍摄时间
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 点赞表
CREATE TABLE likes (
  submission_id INTEGER REFERENCES submissions(id),
  user_id       TEXT,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (submission_id, user_id)
);
```

#### 技术方向

- 图片存储：服务器本地（`~/xiake-media/`）或后期迁移对象存储（腾讯 COS）
- 后端：复用现有 Express 服务器，新增 `/api/submissions/` 路由
- 前端：新增「社区」分页（复用主页分页菜单机制）

#### 状态

> 📋 **规划中** — 待需求细化后进入设计与开发阶段

---

### 需求 31：天气数据源容灾与可迁移能力（Windy → Open-Meteo / 彩云）

**用户故事：** 作为用户，我希望天气预测数据稳定可信，不会出现随机打乱的时间序列；作为维护者，我希望系统可以在多个天气数据源之间平滑切换，降低单一供应商风险。

#### 验收标准

1. 系统应支持至少 2 个可切换天气提供商：`windy` 与 `openmeteo`（首选迁移目标），并预留 `caiyun` 扩展位。
2. 后端应实现统一 Weather Provider 接口，输出统一字段：`hourly[]`、`daily[]`、`sunrise/sunset`、`cloudLow/mid/high`、`humidity`、`visibility`、`wind`、`pressure`。
3. 当上游返回时间序列乱序或重复时，系统应自动执行排序、去重、完整性校验；校验失败需记录告警并回退到候选提供商。
4. 系统应提供运行时配置项（环境变量 + 设置面板只读展示），支持 `primaryProvider` 与 `fallbackProvider`。
5. 系统应在 API 响应中附带 `providerMeta`（provider 名称、更新时间、回退状态），便于排障。
6. 在中国大陆网络环境下，系统应优先验证可达性；当 Open-Meteo 不稳定时，允许切换彩云（若已配置合法 key）。
7. 新旧提供商并行灰度期间，系统应支持双读对比（同坐标同时间）并输出偏差指标（温度、湿度、云量、降水）。
8. 迁移后不得降低现有需求 5/6/7/11/12 对预测算法与展示的能力。

---

### 需求 32：天气迁移优先级调整（仅 Open-Meteo 第一阶段）

**用户故事：** 作为产品负责人，我希望先聚焦一个稳定数据源快速落地，避免同时接入多个供应商带来复杂度。

#### 验收标准

1. 第一阶段仅接入 `openmeteo` 作为天气预测主数据源，`windy` 仅保留地图或历史兼容能力。
2. 第一阶段不实现彩云适配器，不引入彩云 key 配置流程。
3. 保留“未来可接入彩云”扩展点，但不作为当前里程碑阻塞项。
4. 当中国大陆实测可达性/稳定性不达标时，再启动彩云评估与二阶段接入。
5. API 响应应清晰标注当前 provider=`openmeteo`，避免用户误解数据来源。

---

### 需求 33：全球火烧云地图图层（替代 Windy 地图依赖）

**用户故事：** 作为用户，我希望在地图上看到全中国乃至全球的火烧云潜力分布图层，而不是依赖 Windy 原生地图交互。

#### 验收标准

1. 系统应提供独立于 Windy 的底图方案（Leaflet + OSM 或其他可替换底图），用于承载火烧云图层。
2. 系统应支持区域级渲染（中国）与全球级渲染两种模式，并支持缩放联动。
3. 火烧云图层应支持热力色带（低/中/高潜力）与图例说明。
4. 地图平移/缩放后应按视窗重新请求网格数据并重绘图层。
5. 图层渲染方式应支持至少一种：
   - 服务端生成瓦片（推荐）
   - 客户端 Canvas/WebGL 网格渲染
6. 需要控制性能：全球视图请求与渲染时间应可接受（目标 < 3s 首屏可见）。
7. 图层失败不应影响预测列表/卡片等核心功能。

---

### 需求 34：逐步移除 Windy 天气 API 依赖

**用户故事：** 作为维护者，我希望系统按阶段下线 Windy 天气 API，避免乱序数据与密钥管理复杂度，同时保持预测能力稳定。

#### 验收标准

1. 系统应将 Windy 从“天气预测主数据源”降级为“仅历史兼容（短期）”。
2. 新版 `/api/weather/forecast` 默认仅走 Open-Meteo，且返回 `providerMeta.provider=openmeteo`。
3. 系统应在迁移期保留 Windy 回滚开关，但默认关闭，且需记录启用原因与时间。
4. 迁移完成后应移除前端 Windy API Key 输入/存储逻辑（如 `localStorage.user_windy_api_key`）。
5. 迁移完成后应移除后端 `X-Windy-API-Key` 透传逻辑与 `windyService` 主路径调用。
6. 保留地图专题图层能力（需求33），但不再要求 Windy Map Forecast API 作为核心依赖。
7. 下线前必须完成回归验证：需求 5/6/7/11/12 的预测结果质量不低于基线。

---

### 需求 35：光路评分机制物理重构（一步到位）

**用户故事：** 作为用户与摄影决策者，我希望“光路评分”具备可解释的物理含义，不再出现雨雪阴天仍然 100 分的情况，以便我能信任分数并据此出行。

#### 验收标准

1. 系统应废弃“经验型 150/300km 线性评分”作为主算法，不再以固定距离和单阈值线性插值作为核心判据。
2. 系统应基于太阳几何关系进行遮挡计算，至少包含：太阳高度角、太阳方位角、云底高度、沿光路采样距离。
3. 系统应支持沿光路多点采样（至少 3 个采样点，如 20/50/100km），并以加权方式输出可见光路概率。
4. 系统应在降水、雨雪天气码、厚云(overcast)场景触发硬性上限，确保光路分与总分不会出现明显违背常识的高分。
5. 系统应输出结构化解释字段（如 `occlusionProbability`、`blockingLayer`、`capReason`），前端需展示简明可读解释。
6. 前端不得展示与算法不一致的文案（如固定“150km/300km”），展示内容必须与后端返回字段一致。
7. 光路评分对总分的权重应可配置，默认采用保守值（建议 ≤ 0.3），并支持快速关闭光路项用于应急。
8. 系统应提供线上可观测指标：光路分分布、封顶触发率、异常高分告警（如 cloudCover>85 且 lightPath>60）。
9. 需完成回归验证：以历史坏样本（含 Val Thorens 雨夹雪/阴天）验证“不会出现 100 分光路”并形成测试用例。
10. 此需求为“直接重构上线”策略，不采用长期并行双算法；上线后旧算法仅保留短期回滚窗口（可配置，默认关闭）。

---

### 需求 36：Windy 彻底移除（天气预测链路）

**用户故事：** 作为维护者，我希望在验证新链路稳定后，彻底移除 Windy 在天气预测中的代码与配置，减少维护负担与错误来源。

#### 验收标准

1. 预测主链路仅允许 Open-Meteo（weather/prediction 不再引用 Windy provider）。
2. 前端不再存在 Windy API Key 相关设置、存储或提示文案。
3. 后端不再读取或透传 `X-Windy-API-Key`，不再在预测链路调用 windyService。
4. 配置中 Windy 仅可选保留为地图能力（若仍需），不得影响预测结果。
5. 删除/归档 windy 预测相关遗留测试与文档，替换为 Open-Meteo 基线。
6. 上线前完成回归：需求 5/6/7/11/12 全通过，且连续 7 天无 Windy 预测调用日志。
7. 提供一次性迁移脚本清理本地存储旧键（如 `user_windy_api_key`）并记录迁移完成标记。

---

### 需求 37：朝霞/晚霞评分散点地图（中国区域）

**用户故事：** 作为用户，我希望在朝霞/晚霞预测卡片内嵌一张地图，看到中国区域今日评分较高的地点（散点标注），一眼判断哪里最适合拍火烧云。

#### 设计决策（2026-03-17 更新）

- **移除**：热力图（插值大圆圈）方案——视觉效果差、数据精度低
- **采用**：方案B 散点图——评分≥60 的点用小图标标注，干净清爽
- **位置**：嵌入朝霞/晚霞预测卡片，不作为独立模块
- **地域限制**：仅限中国区域（72°E–135°E，18°N–53°N），境外用户隐藏此功能
- **无时间轴**：只展示当日一次生成的结果

#### 验收标准

1. 系统在检测到当前选择位置在中国区域时，朝霞/晚霞预测卡片内显示嵌入地图。
2. 地图上仅标注评分≥60 的采样点，使用 🌅（晚霞）/ 🌄（朝霞）图标，不插值、不画圆圈。
3. 后端每天生成一次评分网格缓存（CST 15:00），约 50–100 个采样点覆盖中国主要区域。
4. 所有用户共享同一份缓存，用户访问不触发新的 API 调用。
5. 日常 Open-Meteo API 消耗不超过 100 次/天（1 次更新 × 100 点/次）。
6. 点击地图上的标注点，弹出简单 tooltip 显示地名（如有）+ 评分。
7. 当前选择位置不在中国区域时，不显示此地图，不消耗 API。
8. 缓存附带生成时间，前端显示"今日数据，更新于 HH:mm"。

---

## 附录：火烧云预测算法原理

> 本章节基于 sunsetbot.top 火烧云预报教程（第1-4章）整理，作为系统评分算法的理论依据。

### A.1 几何模型

火烧云的本质是：太阳在地平线以下时，阳光从云底斜射入射，照亮云层下表面。

**霞光最大深入距离公式**（精确）：

```
L = sqrt(2 × R_earth × H_cloud_base)
```

- R_earth = 6,371,000 m（地球半径）
- H_cloud_base = 云底高度（米）

| 云底高度 | 霞光最大深入距离 |
|---------|---------------|
| 2 km    | ~319 km       |
| 3 km    | ~391 km       |
| 6 km    | ~553 km       |
| 8 km    | ~638 km       |

**阳光路径近似（极坐标抛物线）**：

在以观察者为原点的坐标系中，阳光路径近似为抛物线：

```
h(x) = x² / (2 × R_earth) - x × sin(θ_sun)
```

其中 θ_sun 为太阳高度角（负值表示在地平线以下）。

随太阳下沉，抛物线向远处平移，等效于：
- 晚霞：云边界内侧先烧，向外扩展，内侧先结束
- 朝霞：时序相反

**视线高度角近似**（观察者看远处云的仰角）：

```
θ_view ≈ (H_target - D²/(2R)) / D
```

其中 D = 水平距离，H_target = 目标云底高度。
简化：`θ_view ≈ H_target / D`（地球曲率修正 = D/(2R)）

### A.2 云层分类与火烧云贡献

基于国际云图分类与散射特性：

| 云层 | 高度范围 | 组成 | 火烧云贡献 |
|------|---------|------|-----------|
| 高云（卷云/卷层云） | >6 km | 冰晶 | **最佳**，冰晶折射产生橙红色，权重 0.70 |
| 中云（高积云/高层云） | 2.5-6 km | 过冷水+冰晶 | **良好**，颜色鲜艳，权重 0.45 |
| 低云（层积云/积云） | <2.5 km | 液态水 | 作为遮挡评估，多则遮挡光路，权重 0.10 |

**最佳云量区间：30-70%**
- <10%：云量太少，无充足载体
- >85%：完全遮蔽，光路无法穿透

### A.3 大气消光模型

大气消光遵循比尔-朗伯定律：

```
I = I₀ × exp(-τ / sin(θ_sun))
```

其中：
- τ = 气溶胶光学厚度（AOD）
- θ_sun = 太阳高度角

AOD 与能见度的关系（经验公式）：

```
AOD ≈ 3.91 / (visibility_km × σ_rayleigh_correction)
```

**等效云底高度修正**：

当 AOD 较高时，需要计算气溶胶层对阳光的"等效地表高度"：

```
H_equiv = H_aerosol_scale × ln(σ_surface × H_aerosol_scale / AOD_threshold)
```

实际有效云底高度 = 原始云底高度 - H_equiv，影响是否能出现火烧云。

### A.4 当前系统评分公式

**云画布评分（Cloud Canvas Score）**：

```javascript
cloudCoverScore = cloudCover × CLOUD_WEIGHTS[level]
// CLOUD_WEIGHTS = { high: 0.70, mid: 0.45, low: 0.10 }

optimalBonus = (cloudCover >= 30 && cloudCover <= 70) ? +15 : 0
lowCloudPenalty = lowClouds > 50 ? -(lowClouds - 50) × 0.5 : 0

canvasScore = cloudCoverScore + optimalBonus + lowCloudPenalty
```

**光路评分（Light Path V2 Score）**：

```javascript
// 多采样点（20/50/100km）计算几何可见性
geometryScore = computeGeometryFromSunPosition(lat, lon, sunAzimuth, sunElevation)

// 大气消光
extinctionFactor = exp(-AOD / sin(sunElevation))

// 硬封顶
if (cloudCover > 85) lightPathScore = 0  // 全遮蔽
if (precipitation > 0) lightPathScore = 0  // 降水

lightPathScore = geometryScore × extinctionFactor × 100
```

**最终融合**：

```javascript
FINAL_WEIGHTS = { CLOUD_CANVAS: 0.8, LIGHT_PATH: 0.2 }

finalScore = canvasScore × 0.8 + lightPathScore × 0.2
```

### A.5 预报不确定性来源

根据教程第四章，以下因素会放大预报误差：

1. **云边界位置不确定性**：云边界处湿度梯度平缓时误差大
2. **云边界移动速度**：风速 >15m/s 时不确定性明显增大
3. **云边界与日落方向夹角**：夹角 <10° 时极不稳定
4. **气溶胶光学厚度 AOD**：直接影响等效云底高度修正

---

## 需求 39：网格抓取队列系统（限流稳定版）

**用户故事：** 作为产品负责人，我希望朝霞/晚霞网格抓取采用可观测、可续跑的队列系统，在上游限流（429）场景下仍能稳定完成批量抓取，并实时看到进度与失败原因。

#### 验收标准

1. 系统应将朝霞（sunrise）与晚霞（sunset）抓取任务入队管理，不允许同一时段重复并发刷新。
2. 系统应支持批次级任务状态：`pending / running / success / failed / retrying`。
3. 系统应实现全局串行 worker（默认 1 个），避免多任务同时冲击上游 API。
4. 系统应支持可配置批次大小（如 10）与批次间隔（如 2500ms），并可通过配置调整。
5. 当上游返回 429 时，系统应读取 `Retry-After`（若存在）并执行熔断等待；若不存在则至少等待 60 秒再继续。
6. 任务中断后应支持断点续跑：已成功批次不重复拉取，未完成批次可继续执行。
7. 系统应暴露状态接口，返回：总点数、完成点数、成功/失败点数、当前批次、总批次、ETA、最近错误。
8. 调试页面应展示实时进度面板，并至少每 1 秒更新一次。
9. 系统应区分“网格点位总数”与“有效分数点数”，避免口径混淆。
10. 在 1° 网格模式下，抓取完成后有效率应显著高于历史基线（6.76%），并可观测到改进。
11. 抓取流程应优先拉取“次日朝霞/晚霞”目标时段（夜间触发时），保证业务时效。
12. 当刷新接口超时（如 504）时，若后台任务仍在运行，系统应返回可查询状态而非误判失败。

---

## 需求 38：火烧云全球分享地图

**用户故事：** 作为火烧云爱好者，我希望能上传自己拍摄的火烧云照片，分享到全球地图上，同时能看到来自世界各地的火烧云照片和访客记录。

### 验收标准

1. 系统提供密码保护的后台管理界面（`/admin`），支持上传火烧云照片
2. 上传时优先读取照片 EXIF 地理位置信息，若无则支持在地图上手动指定位置
3. 分享页面（`/gallery`）渲染全屏 Leaflet 世界地图
4. 照片以随机挑选的缩略图圆形头像形式展示在地图对应坐标位置
5. 点击照片缩略图弹出 Modal 显示原图、拍摄时间和地点信息
6. 访客 IP 自动记录并通过 GeoIP 转换为地理坐标，在地图上以橙色小圆点展示
7. hover 访客点显示国家/城市信息
8. 主页底部统计"来访者"数字可点击进入分享页面
9. 顶部竖直菜单栏新增分享页面入口
10. 管理员密码从环境变量 `ADMIN_PASSWORD` 读取
11. IP 存储前做 SHA256 hash 保护隐私
12. 上传文件限制：最大 20MB，仅允许 JPEG/PNG/HEIC 格式
