# 需求文档

## 简介

天气晚霞预测器是一个Web应用程序，通过 **Open-Meteo API** 获取点位气象数据，并通过 **NOAA GFS + CAMS** 批处理网格数据生成未来 48 小时火烧云地图，分析云层、湿度、能见度、气溶胶等参数，预测火烧云（晚霞/朝霞）出现的可能性和质量。

> ⚠️ **架构说明**：单点预测链路仍以 Open-Meteo 为主；地图热力/网格链路升级为 GFS 天气网格 + CAMS 气溶胶网格的后台批处理管线。Windy 仅作为历史/可选能力保留。

## 术语表

- **系统（System）**: 天气晚霞预测器Web应用程序
- **火烧云（Sunset_Glow）**: 日落/日出时分天空中出现的红色或橙色云彩现象
- **预测质量（Prediction_Quality）**: 火烧云出现可能性的评分，范围0-100
- **Open-Meteo**: 免费天气数据API（当前主数据源）
- **GFS**: NOAA Global Forecast System，作为火烧云地图天气网格主输入，提供云量、湿度、降水、辐射、风等字段
- **CAMS**: Copernicus Atmosphere Monitoring Service，作为火烧云地图气溶胶主输入，提供 AOD、沙尘、黑碳、有机物、PM 等字段
- **数据管线（Data Pipeline）**: 后台按配置范围、时效和字段白名单分批下载、解析、评分、出图和清理的任务链路
- **Agent API**: 给大模型、MCP、自动化脚本调用的受控结构化 API，复用同一后端与预测算法
- **API Token**: 后台生成的调用凭证，只在创建时明文展示，服务端仅保存哈希，用于鉴权、限流、审计和吊销

## 需求列表（1-55）

### 需求 1-5：核心功能
1. **API密钥管理** - 后端统一配置，前端无Key输入
2. **位置选择** - 支持地名搜索、当前定位、收藏位置
3. **天气数据获取** - 通过后端代理获取Open-Meteo数据
4. **基本天气显示** - 当前温度、湿度、风速、云量
5. **火烧云预测算法** - 基于云量、湿度、能见度、低云遮挡的评分算法

### 需求 6-10：预测展示
6. **预测结果展示** - 评分、质量等级（优秀/良好/一般）、颜色编码
7. **未来预测时间线** - 未来3-7天的晚霞预测
8. **响应式设计** - 适配移动端和桌面端
9. **数据刷新** - 手动刷新，30分钟缓存
10. **错误处理** - 网络/API/位置解析错误的友好提示

### 需求 11-14：增强功能
11. **天气界面优化** - 7天概览 + 24小时图表（温度、降水、湿度、风速、气压、云量）
12. **朝霞晚霞预测增强** - 独立评分、黄金/蓝调时段、太阳方位角、云层分层
13. **最近搜索历史** - 最近5个城市，LRU策略
14. **多语言支持** - 10种语言（zh-CN, zh-TW, en-US, ja-JP, ko-KR, vi-VN, fr-FR, es-ES, it-IT, ar-SA）

### 需求 15-18：后端与地图
15. **后端代理API** - 后端统一代理天气API，保护密钥
16. **统一设置面板** - 语言、主题、单位、通知、位置解析配置
17. **个性化设置** - 温度单位（℃/℉）、风速单位（km/h/m/s）、主题（亮/暗/自适应）、默认位置
18. **Windy地图集成** - 基于Leaflet + Windy API的地图可视化（可选）

### 需求 19-21：可视化
19. **周边火烧云可视化** - 8方位雷达图展示周边评分分布
20. **火烧云地图覆盖层** - 基于GFS数据的区域热力图（中国/全球）
21. **UI毛玻璃效果** - Glassmorphism设计风格

### 需求 22-25：架构与配置
22. **前后端分离架构** - 预测算法后端化，提供 `/api/prediction/*` 接口
23. **测试覆盖率达标** - Statements≥80%, Branches≥75%, Functions≥90%, Lines≥80%
24. **中国定位服务** - 支持高德地图（后端代理）、Nominatim（后端/直连）
25. **用户Windy API Key**（已废弃）- ~~用户可配置个人Windy Key~~

### 需求 26-29：UI与数据
26. **主页分页菜单** - 预测功能页 + 火烧云计算方法页
27. **品牌升级** - 霞客/Sunset Voyager Logo
28. **设置界面重组** - 分Section：语言与显示、默认位置、位置解析、通知、高级
29. **访客计数持久化** - SQLite存储，路径 `~/.xiake/visitor.db`

### 需求 30-33：社区与地图
30. **日落投稿社区**（规划中）- 照片上传、社区浏览、互动
31. **天气数据源容灾** - Open-Meteo主数据源，支持故障切换
32. **Open-Meteo第一阶段** - 仅接入Open-Meteo，彩云暂缓
33. **全球火烧云地图图层** - Leaflet + OSM底图，服务端瓦片渲染

### 需求 34-36：Windy下线
34. **逐步移除Windy天气API** - Windy降级为emergency fallback
35. **光路评分物理重构** - 基于太阳几何关系的遮挡计算，雨雪/厚云硬封顶
36. **Windy彻底移除** - 预测链路完全移除Windy依赖

### 需求 37-38：地图与社区
37. **朝霞/晚霞评分散点地图** - 中国、日本、韩国区域评分≥60的采样点标注
38. **火烧云全球分享地图** - 照片上传、全球地图展示、访客记录

### 需求 39-40：队列与配额
39. **网格抓取队列系统** - 限流稳定版，支持429熔断、断点续跑、进度观测
40. **Open-Meteo配额统计与额度保护** - 日调用量统计，软上限（9000/10000）暂停网格抓取

### 需求 41-42：管理后台增强
41. **API调用日志记录** - 分类记录所有外部API调用（火烧云网格grid/天气查询weather/高德地理编码gaode/高德瓦片gaode_tile），管理后台分Tab展示，含时间/接口/参数/状态/耗时/错误
42. **定时更新配置** - 管理后台可配置火烧云数据更新策略（多个时间点，每个可选朝霞/晚霞/都更新），配置持久化到 `~/.xiake/schedule-config.json`

### 需求 43-55：开放能力、多端产品、分享地图、地图数据管线、统一账号与运营分析
43. **气溶胶散射评分** - 接入 Open-Meteo Air Quality API，将 AOD/PM/Dust 纳入评分和展示
44. **全球地理编码重排** - Open-Meteo/Nominatim/Gaode 合并排序，国际城市别名优化
45. **受控 Agent API** - Agent forecast/explain/geocode/map-summary、Token、限流、审计和申请流程
46. **预测卡片概念界面重构** - 朝霞/晚霞卡片信息架构与形成条件分析重构
47. **英日韩西全站功能 i18n 信任任务** - 主外语 key 完整性、残留中文和布局防回归
48. **预测解释与图表可读性优化** - 分数明细 ledger 与 24 小时天气标签
49. **.kiro 三大文档优化** - 三文档边界、追溯关系和近期实现同步
50. **管理后台信息架构重构** - Dashboard/Ops/Logs/Schedule/Agent/Photos 面板化
51. **照片分享元数据增强与地图聚合展示** - 照片元数据、后台编辑、EXIF/地理解析、地图聚合
52. **微信小程序与未来 iOS 产品线** - 原生小程序优先，共用后端 API，未来 iOS 复用契约与设计语言
53. **GFS+CAMS 火烧云地图数据管线** - 地图从 Open-Meteo 点位扫网格升级为 GFS+CAMS 原始网格批处理，后台可切换、可配置范围、可追溯下载量和处理进度
54. **统一用户账号与第三方登录** - 网页端支持微信扫码登录与 Google 登录，小程序端支持微信登录；通过 `users + user_identities + sessions` 打通 Web、小程序和未来 iOS，登录后照片上传、留言反馈和 API 申请统一归属 `userId`
55. **后台访客与运营分析** - 后台从简单访客计数升级为运营分析面板，统计 UV/PV、来源渠道、预测查询、热门地点、照片上传、反馈/API 申请、转化漏斗、错误率和慢请求；隐私上只存 hash 与粗粒度地区，不保存精确个人轨迹

### 需求 43：气溶胶/空气颗粒纳入评分与 UI
43. **气溶胶散射评分** - 接入 Open-Meteo Air Quality API，将 `aerosol_optical_depth`、`dust`、`pm2_5`、`pm10`、`us_aqi/european_aqi` 按小时合并进天气数据。评分上将能见度作为“通透度结果”，气溶胶作为“散射/灰霾原因修正”，避免重复计权。UI 需在实时天气、分数明细、文字分析、算法说明中展示气溶胶/颗粒物影响。无气溶胶数据时必须降级到现有逻辑。

### 需求 44：国际城市搜索排序优化
44. **全球地理编码重排** - 城市搜索不能靠维护全世界城市表。Auto 搜索应统一合并 Open-Meteo/Nominatim/Gaode 结果后重排：全球源作为国际主源，高德只对明显中国查询加权；排序考虑 exact/alias/contains、人口、首都/行政中心、国家/行政区、用户语言、provider 置信度。保留中国、美国、欧洲主要城市的高频中文名、英文名、常见缩写/别名（如 LA/NYC/SF/DC/HK/洛杉矶/纽约/伦敦/巴黎/柏林/罗马/马德里/阿姆斯特丹等），并覆盖中文、英文国际城市测试。

### 需求 45：Agent API 与 API Token 管理
45. **受控 Agent API** - 在同一个 Node 后端内新增 `/api/agent/*` 结构化接口，面向 Alex 自用 agent 与未来邀请用户开放（禁止商用）。接口必须复用现有天气、地理编码、预测与解释逻辑，不复制算法；必须通过 `Authorization: Bearer xiake_live_xxx` 或等价 header 鉴权；后台必须支持 Token 创建、命名、scope、每分钟/每日额度、启停/吊销、最近使用时间、调用次数和错误统计。Token 明文只在创建时显示，服务端只保存哈希。第一阶段优先实现 `/api/agent/forecast`，后续扩展 `/api/agent/explain`、`/api/agent/geocode`、`/api/agent/map-summary` 与 OpenAPI 文档。 主页需新增「API接入」主页面/菜单入口，面向开发者和大模型用户说明申请 Token、调用方式、接口示例、错误码、限流规则和安全注意事项；同时新增「API申请」入口，用户仅提交邮箱/联系信息与简单用途说明，后台可查看申请并人工创建 Token。

**验收口径**：
- 所有 `/api/agent/*` 业务接口默认拒绝无 Token 请求，返回稳定错误码 `UNAUTHORIZED`。
- Token 可在后台创建、停用、吊销、调整额度；明文只展示一次，列表不返回明文。
- Agent forecast 必须支持城市名和经纬度两种输入，返回机器可读 JSON，不要求大模型解析网页文本。
- 主页必须提供「API接入」入口，展示快速开始、Token 使用、curl 示例、返回字段解释、错误码和额度说明；页面必须嵌入现有霞客主题框架，复用当前菜单、卡片、按钮、字体、明暗主题和移动端布局。
- 主页必须提供「API申请」入口，最小表单字段为邮箱/联系方式，可选填写用途；提交后进入后台申请列表，管理员审核后可基于申请创建 Token，申请与 Token 关联持久化保存。API 使用条款必须明确禁止商用，申请页需展示“仅限个人/研究/测试/非商业用途”。申请页必须在现有主题框架内实现，不允许独立跳转成割裂风格页面。
- Agent API 与网站预测共用同一套算法和数据源，分数、时间窗口、解释不能分叉。
- 必须有鉴权、限流、成功调用、错误调用的自动化测试。
- 需求45 的测试覆盖必须显著高于普通功能：覆盖鉴权失败、权限不足、限流、申请流程、后台审核、Token 明文不泄露、schema 稳定、非商用文案和移动端 UI。

## 需求46：朝霞/晚霞预测卡片概念界面重构（2026-04-25）

### 背景
Alex 提供新的朝晚霞预测概念界面，目标是借鉴其信息结构，而不是照搬视觉皮肤。新版应解决当前“火烧云形成条件分析”信息像日志堆叠、结论不突出、指标难扫读的问题。

### 范围约束
- 只重构朝霞/晚霞预测卡片与“火烧云形成条件分析”信息层级。
- 保持霞客现有视觉体系：深色/玻璃感/霞光橙金/蓝紫天空，不采用概念图的粉紫 App 风。
- 云况雷达保持不变；不修改地图、算法、接口和云况雷达交互。
- 时间展示必须沿用地点实际 IANA 时区，日出/日落时间不是倒计时。

### 用户故事
1. 作为普通用户，我希望打开预测卡片时先看到明确结论、分数和日出/日落时间，从而快速判断是否值得出门。
2. 作为摄影/观霞用户，我希望能快速扫读高云、中云、低云、能见度、湿度、AOD 等关键因素，而不是阅读长段日志。
3. 作为移动端用户，我希望朝霞/晚霞卡片在小屏幕上仍然清晰、不截断、不挤压云况雷达。

### 验收标准
- 卡片顶部必须明确显示：朝霞/晚霞类型、日期语义（今日/明日/具体日期）、最终分数、质量等级、日出/日落时间、最佳观赏窗口。
- 日出/日落时间必须按预测地点法定/IANA 时区显示；不得使用用户浏览器、服务器或经度粗算作为主逻辑。
- 云层信息必须以结构化方式展示高云/中云/低云，百分比醒目且可扫读。
- 形成条件分析必须分层：顶部结论，中部指标网格，底部 2-3 条简短解释；避免长句堆叠。
- 指标状态图标统一为 ✅ / ⚠️ / ❌，减少混用 ℹ️ / 🔥 / ✨ 等装饰符号。
- 数值需要加粗或右对齐，避免像日志文本。
- 不得截断长文字分析；不得用 `max-height + overflow:hidden` 隐藏内容。
- 云况雷达视觉、布局和数据逻辑保持不变。
- 必须补/更新 DOM 渲染回归测试：复用既有时区、AOD、分数明细和云层测试，不重复测试算法；新增/更新新 UI 结构、指标网格、分数明细入口和云况雷达不受影响的展示断言。

## 需求47：英日韩西全站功能 i18n 信任任务（2026-05-02）

### 背景
全站用户可见功能需要在 `en-US`、`ja-JP`、`ko-KR`、`es-ES` 四种主外语下保持可信：不能漏 key、不能 fallback 中文、不能因长文案破版。后续新增功能默认同步 `zh-CN`、`zh-TW`、`en-US`，主外语完整性由自动化测试守住。

### 验收标准
- 以 `zh-CN` 为基准，主语言 key 结构一致，无 `Translation key not found`。
- 静态页面、动态控制器/组件/服务文案均有审计清单或明确豁免。
- 英/日/韩/西核心路径不出现中文残留（品牌名、地名数据、明确允许项除外）。
- 新增页面/大块文案优先维护 `zh-CN`、`zh-TW`、`en-US`；其他语言无明确要求时 fallback 英文，避免低质量机器翻译。
- CI 必须包含 i18n key completeness、质量 guard 和关键页面布局防回归。

## 需求48：预测解释与图表可读性优化（2026-05-07）

### 背景
近期用户反馈集中在两个可读性问题：分数明细虽然展示了计算链路，但“为什么从 78 变成 60”不够直观；24 小时温度图只显示温度趋势，缺少同一时间的天气状态。需求48 用于统一收口“预测结果为什么这样”和“天气趋势如何扫读”的体验优化。

### 范围约束
- 只优化展示层和解释层，不改变评分算法、Open-Meteo 数据源、地图队列和后端接口契约。
- 分数明细必须展示关键计算链路，但避免“分数流水/不是文字分析/未触发封顶”这类解释 UI 自述或无信息量文案。
- 温度图天气标签复用已有天气文案与主题 token，不引入 emoji 作为正式图标体系。

### 用户故事
1. 作为普通用户，我希望点击分数后能直接看懂基础分如何经显色条件变成最终分。
2. 作为摄影用户，我希望能看到云层载体、光路、基础分、显色修正、最终分之间的关系，而不是只看到孤立数字。
3. 作为查看 24 小时预报的用户，我希望温度趋势旁边能同时看到晴/少云/多云/阴天/降水，便于快速判断出行窗口。

### 验收标准
- 分数明细顶部摘要在无硬封顶时展示“基础分 → 显色修正后分”的链路。
- 基础分行必须可读地展示 `画布×80% + 光路×20% = 基础分`。
- 显色修正行必须可读地展示 `基础分 × 显色系数 = 修正后分`。
- 未触发封顶/保底时不渲染“修正/无修正/未触发”占位行。
- 24 小时温度图下方展示天气标签，来源于同一小时的降水与云量字段；移动端降采样显示，避免横向溢出。
- 相关渲染逻辑必须有单元测试覆盖。

## 需求50：管理后台信息架构重构（2026-05-07）

### 背景
管理后台原先把访问统计、系统健康、运维动作、API 日志、定时配置、Token、申请审核、Agent 审计和照片管理全部堆在一个页面，信息层级混乱，高风险动作也和普通查看混在一起。

### 范围约束
- 使用霞客主页同源模板与主题变量，复用 `home-view-menu` 风格的页面切换，不另起一套视觉语言。
- 后台拆分为总览、运维、日志、定时任务、Agent/API、照片多个面板。
- 保留现有 API 与核心业务逻辑，优先改前端信息架构和交互。
- 风险操作必须集中在运维页，并保留二次确认。

### 验收标准
- `/admin` 不再是所有功能堆叠的单页长滚动。
- 顶部菜单可切换各后台功能页，并支持 hash 深链。
- Dashboard/运维/日志/定时/Agent/照片功能可分别加载与刷新。
- Grid 队列状态在运维页可视化展示。
- Token 编辑不再使用 `prompt`，改为弹窗表单。
- 霞客主题变量调整能作用到后台主要卡片、菜单和面板。

## 需求51：照片分享元数据增强与地图聚合展示（2026-05-09，2026-05-11 更新）

### 背景
分享地图需要从“照片点位展示”升级为更像相册地图的体验。照片不仅要有坐标，还要清楚展示上传者、拍摄地点、拍摄时间、上传时间；同一地点多张照片不能堆叠成不可点击的 marker。

### 当前字段口径
- 上传者字段使用 `uploaderName`；为空时展示层 fallback 为“网友”。
- 拍摄地点字段使用 `locationName`；可由经纬度反向地理编码建议，也可手动填写。
- 拍摄时间字段使用 `takenAt`；优先从 EXIF 读取，用户可手动修正或清空。
- 上传时间字段使用 `uploadedAt`；由服务端记录，后台只读，不允许手动修改。
- 地图坐标字段仍为 `lat/lon`；修改 `locationName` 不应自动改变坐标，修改坐标才表示改变地图位置。

### 范围约束
- 分享地图和火烧云评分地图只共用底图，不共享分数图例、点击查分、实时天气等操作逻辑。
- 后台照片上传/编辑允许字段为空；旧照片缺少新增字段时必须可读。
- 地址解析必须复用和主页一致的 `/api/geocoding/search` 路线；使用高德等外部 API 时必须进入后台 API 调用日志。
- EXIF 经纬度/拍摄时间需要支持“重新解析”，用于手动修改后恢复照片原始信息。

### 验收标准
- 后台上传照片支持填写/编辑 `uploaderName`、`locationName`、`takenAt`、`lat/lon`、描述；`uploadedAt` 只读展示。
- 上传表单提供“解析地址 / 解析经纬度 / 解析拍摄时间”操作。
- “解析地址”从输入地点解析经纬度；“解析经纬度/拍摄时间”从当前选中图片重新读取 EXIF。
- 公开 `/api/photos` 返回展示所需元数据，但不暴露 `uploadIpHash`、`uploadDay` 等内部限额字段。
- 分享地图 popup 展示拍摄地点、拍摄时间、上传时间、上传者。
- 同一地点/近距离多图按 zoom 和像素距离聚合，低 zoom 聚合，高 zoom 逐步拆分。
- marker、popup、聚合列表优先使用 `thumbUrl`，无缩略图才 fallback 原图。

## 需求52：微信小程序与未来 iOS 产品线（2026-05-11）

### 背景
霞客如果面向普通用户高频使用，公众号更适合作为内容分发和导流，不适合作为主产品入口。主产品形态应优先做原生微信小程序；后续 iOS 复用同一套后端 API、产品信息架构和设计语言。

### 产品方向
- 正式方向选择 **B：原生小程序版**，不是单纯 `web-view` 套壳。
- `web-view` 只可作为临时入口或过渡页，不作为正式 MVP 的主体体验。
- 小程序目标是霞客 Web 的同等原生端：同一设计语言、同一核心功能、同一评分和解释口径；不得做成简化查分工具。
- 公众号保留为文章、活动、菜单跳小程序的导流入口。
- 未来 iOS 不复用小程序代码，但复用后端 API、页面结构、设计 token、评分展示口径和分享卡片规范。

### 仓库与存储
- 小程序代码放在当前仓库 `miniprogram/`，不单独建库。
- 后端继续沿用现有 `server/` 与 `https://sunset.bjhyc.online/api/...`。
- 小程序登录可从微信 `openid` 起步，但服务端业务数据必须预留 `userId + identity provider` 抽象，避免未来 iOS 只能绑定微信身份。
- 用户收藏、最近查询、投稿归属等服务端数据由服务端持久化；小程序本地只保存缓存与最近状态。
- 新增小程序/未来 iOS API 必须先稳定接口契约，再分别接入前端。
- 小程序正式开发前必须先冻结 MVP 信息架构、共享 API 契约、用户身份模型和平台配置清单。

### MVP 范围
1. 首页查火烧云：支持手动搜索地点、当前位置、朝霞/晚霞切换。
2. 今日/明日评分：展示最终分、质量等级、最佳观赏窗口、关键解释。
3. 火烧云文字分析：结果页必须展示云况画布、光路条件、色彩修正等核心分析，不得只展示一句泛化解释。
4. 周边云况雷达：结果页必须展示周边 8 方向云况/分数，用原生小程序布局承载，不直接复用 Web DOM。
5. 三天预测：结果页必须展示未来 3 天朝霞/晚霞评分，分档颜色与 Web 端一致。
6. 城市收藏与最近查询：小程序端展示，服务端按 `userId` 持久化。
7. 分享卡片：支持微信小程序卡片分享，文案和缩略图遵循霞客设计语言。
8. 分享地图入口：第一阶段可跳转 H5 或做轻量原生列表/地图，后续原生聚合。
9. 照片上传：使用小程序 `wx.chooseMedia` / `wx.uploadFile` 接入现有照片 API。

### 平台约束
- 小程序请求、上传、下载、`web-view` 都必须在微信后台配置合法域名或业务域名。
- 所有线上请求必须走 HTTPS 域名，不能使用 IP、非备案域名或本地端口。
- `wx.getLocation`、`wx.chooseLocation` 等定位能力需要类目/接口权限与 `app.json` 声明；提审前必须确认权限。
- 小程序地图和图表不能直接复用当前 DOM/Leaflet/Chart.js 实现，需要单独适配。
- 使用定位、相册、上传、用户身份前必须补齐隐私协议、授权说明、数据删除路径和审核材料。
- 小程序测试必须覆盖开发者工具、真机调试、体验版和自动化测试；涉及定位、相册、上传、地图、分享的能力不能只在模拟器验收。

### 验收标准
- `miniprogram/` 可以在微信开发者工具打开，不污染现有 Web 构建和部署流程。
- 小程序所有业务数据来自现有后端 API 或新增共享 API，不复制预测算法。
- 后端新增接口同时考虑未来 iOS 调用，不写死微信端专属响应结构。
- API 契约必须覆盖登录、地点搜索、预测、收藏/最近查询、照片上传、分享和分享地图入口，并明确鉴权、错误码、限流和字段兼容规则。
- 小程序 UI 复用霞客设计语言：颜色、层级、卡片、评分表达、文案风格一致。
- 设计 token 必须能映射到 Web CSS、小程序 WXSS 和未来 iOS，避免三端各自发明颜色/字号/层级。
- 小程序操作逻辑必须跟 Web 主流程类似：地点输入/定位 -> 朝霞晚霞/日期选择 -> 查询 -> 结果详情 -> 分享/收藏/地图/算法说明，不得把核心入口藏到弱链接或纯 H5 跳转。
- 小程序 MVP 至少覆盖搜索、定位、评分展示、火烧云文字分析、周边云况雷达、三天预测、分享、收藏/最近查询、照片上传中的核心路径。
- 小程序不得降级成“查分壳”：网页版已有核心判断能力更新后，小程序必须同步对应 API 字段、评分档位、解释口径和结果页信息结构。
- 小程序与 Web 的差异只能来自平台能力约束，例如 Leaflet/DOM 改为小程序 `map`/canvas/WXML；不能因为实现省事删掉 Web 已有的核心功能。
- 小程序提审前必须通过真机验收矩阵：iOS/Android、定位授权/拒绝、相册上传、分享卡片、地图 marker 性能、弱网和接口失败降级。
- CI 需要覆盖小程序 service 单测、共享 API 合约测试，并预留 `miniprogram-ci` 构建/预览与 `miniprogram-automator` 页面自动化入口。
### PR #693 baseline update (2026-05-13)
PR #693 (`fix/miniprogram-web-parity-ui`, merge `709dd89`) is included in Requirement 52. It already covers the first web-parity shell pass: home menu/settings, direct shortcut query, result product-loop navigation, and compass-style surrounding cloud radar.

Acceptance update:
- Future Mini Program parity work must start from the PR #693 code baseline.
- Do not replace completed PR #693 shell behavior unless web-vs-Mini-Program screenshot review shows a specific mismatch.
- Remaining parity work should focus on screenshot-level home/result polish, map/gallery/upload parity, methodology/API/settings surface review, and real-device validation.
- 所有新增用户可见文案至少维护 `zh-CN`、`zh-TW`、`en-US`，其他语言默认英文 fallback。

## 需求54：统一用户账号与第三方登录（2026-05-31）

### 背景
霞客需要把 Web、小程序和未来 iOS 的用户能力收敛到同一个账号体系。用户可以在网页端用微信或 Google 登录，在小程序端用微信登录；登录后上传照片、留言反馈、申请 API 都应归属同一个 `userId`，避免同一个人在不同端产生割裂账号。

### 产品方向
- 统一账号中心：`users` 表代表真实用户，业务数据只引用 `userId`。
- 多身份绑定：`user_identities` 记录 `wechat_web`、`wechat_miniprogram`、`google`，未来可扩展 Apple、手机号、邮箱。
- 微信 Web 和微信小程序优先用同一微信开放平台主体下的 `unionid` 自动打通；拿不到 `unionid` 时必须提供安全的手动绑定路径。
- Web 端登录方式：微信开放平台「网站应用」扫码登录、Google OAuth/OIDC 登录。
- 小程序端登录方式：`wx.login()` 获取 code，后端 `code2Session` 换 `openid/unionid`。
- 登录态分端处理：Web 使用安全 session cookie，小程序使用服务端签发的 session token；业务层都解析为同一个 `userId`。
- 管理后台 Basic Auth/Admin token 与普通用户登录体系保持隔离。

### 申请与平台配置
- 微信开放平台账号必须完成主体认证，并把网站应用和小程序绑定到同一个开放平台主体。
- 微信网页登录需申请开放平台「网站应用」，回调域名使用 `sunset.bjhyc.online`，生产配置 `WECHAT_WEB_APP_ID`、`WECHAT_WEB_APP_SECRET`。
- 小程序后台需提供 `WECHAT_MINI_APP_ID`、`WECHAT_MINI_APP_SECRET`，并在微信后台配置 request/upload/download 合法域名。
- Google Cloud 需创建 Web OAuth Client，回调地址为 `https://sunset.bjhyc.online/auth/google/callback`，生产配置 `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`。
- 生产必须配置 `AUTH_SECRET` 或等价 session 签名密钥；所有第三方 secret 只允许存在后端环境变量或服务器安全配置中。
- 登录、照片上传、留言反馈和 API 申请上线前必须补齐隐私政策、用户协议、数据删除或人工删除入口。

### 登录后能力
- 照片上传：必须绑定 `userId`，可继续保留审核/隐藏/限流机制。
- 留言反馈：建议登录后绑定 `userId`；如允许匿名反馈，后续登录后也应可关联用户主动提交的反馈。
- API 申请：申请记录、审核状态和生成的 API token 归属 `userId`；不再只依赖邮箱文本匹配。
- 收藏、最近查询、照片投稿等已有小程序用户数据继续引用 `userId`，不得直接引用 `openid` 作为业务主键。

### 验收标准
- 同一个微信用户在 Web 微信登录和小程序微信登录时，如能拿到同一 `unionid`，必须落到同一个 `userId`。
- Google 登录创建或绑定到 `google sub` identity；邮箱只作为展示/联系字段，不能单独作为不可变主键。
- Web session cookie 必须设置 `HttpOnly`、`Secure`、`SameSite`；小程序 token 不暴露第三方 `openid`、`unionid`。
- 第三方回调必须校验 `state`、回调域名和 token 签名，不接受前端伪造身份。
- 账号合并必须有冲突保护：一个 identity 只能绑定一个 user；手动绑定需要用户显式确认。
- 照片上传、留言反馈、API 申请的接口测试必须覆盖未登录拒绝、已登录归属、跨用户不可访问和旧数据兼容。
- 新增用户可见文案必须同步 `zh-CN`、`zh-TW`、`en-US`；登录失败、绑定冲突、授权取消要有可理解提示。

## 需求55：后台访客与运营分析（2026-05-31）

### 背景
现有访客能力主要是计数和分享地图访客点，无法回答“用户有没有真的用起来、从哪里来、查了什么、卡在哪里、哪些功能有价值”。后台需要新增访客与运营分析能力，帮助 Alex 判断 Web、小程序、API、照片、反馈和 API 申请的真实使用情况。

### 产品方向
- 后台新增「访客分析」或「运营分析」面板，不和系统健康、数据管线状态混在一起。
- 第一阶段以自有轻量统计为主，不引入重型第三方埋点；保留后续接入专业分析工具的边界。
- 分析对象覆盖 Web、小程序、Agent/API、照片、反馈、API 申请等关键路径。
- 数据采集必须隐私优先：IP 只存 hash，地区只到国家/省市或城市级，精确经纬度只用于用户主动查询/上传业务，不作为访客轨迹长期保存。

### 指标范围
- 总览指标：今日/昨日/7日/30日 UV、PV、新访客、回访访客、人均访问页数、平均停留时间、预测查询量、小程序访问量、Agent/API 调用量。
- 来源分析：Web/小程序/API/后台渠道，直接访问/分享链接/搜索/公众号或小程序入口，设备类型 iOS/Android/Desktop，粗粒度地区。
- 行为分析：热门搜索地点、朝霞/晚霞切换比例、日期选择比例、查看火烧云地图、上传照片、分享、留言反馈、API 申请等事件。
- 转化漏斗：首页访问 -> 地点查询 -> 查看预测 -> 查看地图/分享/上传照片/申请 API。
- 质量与异常：接口失败率、慢请求 Top、地理编码失败 Top query、小程序错误日志、火烧云图层加载失败、API token 使用异常。

### 隐私与保留策略
- 默认不记录完整 IP、精确个人轨迹、第三方登录 openid/unionid、session token 或 API token 明文。
- userId 维度分析只用于登录后归属和安全审计；后台展示以聚合指标为主，避免直接展示个人行为时间线。
- 原始事件可短期保存，聚合指标长期保存；保留天数应可配置，默认建议原始事件 30 天、日聚合 1 年。
- 提供数据删除或人工删除入口，能删除指定 `userId` 的访客/行为事件。

### 验收标准
- 后台能查看今日/昨日/7日/30日 UV、PV、预测查询量、照片上传、反馈、API 申请和 Agent/API 调用量。
- 后台能按渠道区分 Web、小程序、API、后台，并展示热门地点和主要入口来源。
- 后台能展示至少一个核心漏斗：访问首页 -> 查询地点 -> 查看预测 -> 分享/上传/API 申请。
- 后台能展示接口失败率、慢请求 Top、地理编码失败 Top query 和小程序错误摘要。
- 统计接口必须走后台鉴权；公开前端不得读取运营分析数据。
- 采集逻辑必须有隐私测试：不落明文 IP、不落 token/openid/unionid、不保存精确访客轨迹。
- 新增后台 UI 文案必须同步 `zh-CN`、`zh-TW`、`en-US`，移动端后台不能溢出。

## 需求53：GFS+CAMS 火烧云地图数据管线与后台控制台（2026-05-26）

### 背景
现有火烧云地图使用 Open-Meteo 点位/批量 API 扫网格，受免费额度和点位调用模型限制，只适合 1° 粗网格或小范围刷新。地图产品需要更密的区域覆盖、未来 48 小时连续时效、可追溯任务状态和可控磁盘占用。当前正式部署目标为腾讯云 CVM `SA2.LARGE4`（北京 `ap-beijing-7`，4 核 AMD EPYC 7K62，约 3.6GiB RAM + 2GiB swap，40G 系统盘、约 18G 可用），因此必须使用单 worker、分批、流式、边处理边清理的网格数据管线。

### 产品方向
- 单点预测继续走现有 `ProviderOrchestrator` 与 Open-Meteo 主链路。
- 火烧云地图改为内部数据管线：GFS 提供天气网格，CAMS 提供气溶胶网格，统一裁剪、缓存、评分和出瓦片。
- 后台必须能管理数据源模式、拉取范围、未来时长、分辨率、手动刷新、重试、暂停、清理和回滚。
- Open-Meteo 不再作为地图网格主抓取源，只保留点位详情、legacy grid fallback、人工对照和故障恢复用途。
- 后台必须提供统一缓存管理视图，能同时看到 GFS/CAMS 新管线、Open-Meteo 旧网格链路、当前公开地图实际读取的缓存源，以及新老模式切换状态。

### 范围配置
- 后台必须支持区域预设：`china`、`east_asia`、`test_small`、`custom_bbox`。
- 自定义 bbox 至少包含 `north/south/west/east`，保存前必须校验经纬度范围、面积上限和格点数上限。
- 默认配置：`china`、`0.5°`、未来 `48h`、GFS+CAMS、仅保留未来 48-72 小时评分结果。
- 允许升级到 `0.25°`，但必须经过资源预估；如果预计下载量、格点数、临时文件或剩余磁盘不安全，后台应拒绝启动任务。
- 任务运行时必须使用同一份配置，不允许下载器、评分器和瓦片服务各自读取不同范围。

### 数据与资源约束
- GFS 字段白名单第一版至少包含：`TCDC/LCDC/MCDC/HCDC/RH/VIS/APCP/PRATE/PWAT/DSWRF/TMP/UGRD/VGRD`。
- CAMS 字段白名单第一版至少包含：`total_aerosol_optical_depth_550nm`、`dust_aerosol_optical_depth_550nm`、`black_carbon_aerosol_optical_depth_550nm`、`organic_matter_aerosol_optical_depth_550nm`、`sulphate_aerosol_optical_depth_550nm`、`particulate_matter_10um`。
- 处理必须按 cycle 和 forecast hour 分批进行：下载一个小批次、解析、评分、写缓存、删除原始文件，再进入下一批次。
- 网站、小程序和单点预测 API 的可用性优先级高于地图管线；GFS/CAMS 管线不得由用户请求触发，只能由后台手动操作或定时任务启动。
- 管线默认单 worker，常驻内存预算 512MB、硬上限 768MB，并预留约 2GB 给 Node API、静态站点、小程序接口、系统缓存和数据库/日志写入。
- 原始 GRIB/NetCDF 文件不得长期保存；处理成功后立即删除，失败时最多保留到后台配置的调试窗口。
- 默认存储策略：`raw` 最多 1 小时、`tmp` 最多 3 小时、评分缓存 3 天、瓦片 3 天、日志 7 天。
- 当全盘剩余低于 3GB，或 `raw/tmp` 预计超过 5GB，任务必须暂停并提示清理。

### 后台控制台
- `/admin` 的运维/数据面板必须展示当前数据源模式：`openmeteo`、`gfs_cams`、`hybrid`、`cache_only`、`paused`。
- 必须展示最近一次成功产物的来源、cycle、bbox、分辨率、forecast window、生成时间和可用时效范围。
- 必须展示当前任务阶段：`queued`、`downloading`、`parsing`、`scoring`、`tiling`、`cleanup`、`completed`、`failed`。
- 必须展示下载与处理追溯：source、cycle、forecast hour、变量、区域、文件大小、耗时、成功/失败、错误原因。
- 必须展示每日统计：下载次数、下载 MB/GB、处理 forecast hour 数、生成瓦片数、失败次数、重试次数。
- 必须展示统一缓存状态：公开地图当前 source/mode/status、GFS/CAMS 产品数量与点数、产品缓存占用、Open-Meteo sunrise/sunset 刷新进度、缓存点数、stale 状态和最近错误。
- 必须能从同一面板触发 Open-Meteo legacy grid 刷新，用于 GFS/CAMS 产物未就绪时拉取、插值并写入旧版 Grid 缓存。
- 管理操作至少包括：手动刷新未来 48h、暂停/恢复、重试失败任务、清理旧缓存、切换数据源模式、回滚到上一个成功 cycle。

### API 与持久化
- 新增或复用后台 API：`GET /api/admin/data-pipeline/status`、`GET /api/admin/data-pipeline/config`、`POST /api/admin/data-pipeline/config`、`POST /api/admin/data-pipeline/estimate`、`GET /api/admin/data-pipeline/runs`、`GET /api/admin/data-pipeline/runs/:id`、`POST /api/admin/data-pipeline/run`、`POST /api/admin/data-pipeline/runs/:id/retry`、`POST /api/admin/data-pipeline/cleanup`。
- `GET /api/admin/data-pipeline/status` 必须返回 `cacheManagement` 聚合对象，包含 `activeMap`、`pipelineRun`、`pipelineProducts`、`legacyOpenMeteo` 和 `switching`，供后台统一渲染新老缓存状态。
- 配置建议保存到 `~/.xiake/data-pipeline-config.json`；运行记录和步骤记录可先用 JSONL/SQLite，必须可被后台查询。
- 每次下载、解析、评分、出瓦片、清理都必须先写 step 记录，再执行动作，完成后更新 bytes、耗时和输出路径。
- 失败记录必须包含 `retryable`、`errorCode`、`message`、`source`、`cycle`、`forecastHour` 和相关路径；不得只在控制台输出。

### 验收标准
- 腾讯云 `SA2.LARGE4`（3.6GiB RAM、2GiB swap、40G 系统盘且约 18G 可用）环境下，默认中国/0.5°/48h 任务可以完成，不因一次性读取全量 GRIB/NetCDF 导致 OOM 或磁盘打满。
- 后台可以切换地图数据源模式；切换到 `cache_only` 后地图只读最近成功产物，不再启动外部下载。
- 后台可以配置拉取范围并看到资源预估；危险配置必须被拒绝且说明原因。
- 后台统一缓存管理必须能回答：当前公开地图实际读 GFS/CAMS 还是 Open-Meteo fallback；GFS/CAMS 新产物是否存在；Open-Meteo 旧网格是否正在刷新、插值进度多少、缓存是否可用。
- 任务进度可追溯到 GFS/CAMS 的每个下载与处理步骤；刷新页面后状态仍可恢复。
- 原始文件处理后会自动清理；清理动作本身有记录。
- `/api/heatmap` 与 `/api/tiles` 优先读取 GFS+CAMS 产物；产物缺失时按当前降级策略返回缓存/错误，不重新触发 Open-Meteo 大规模扫点。
- 自动化测试至少覆盖配置校验、资源预估、状态机、运行日志、清理策略、数据源模式切换和失败重试。

## 附录：火烧云预测算法原理

### 评分公式

**云画布评分**：
```
cloudCoverScore = highClouds × 0.70 + midClouds × 0.45 + lowClouds × 0.10
optimalBonus = (cloudCover 30-70%) ? +15 : 0
lowCloudPenalty = (lowClouds > 50) ? -(lowClouds - 50) × 0.5 : 0
```

**光路评分（V2）**：
```
geometryScore = 基于太阳高度角、云底高度、沿光路采样（20/50/100km）
extinctionFactor = exp(-AOD / sin(sunElevation))
lightPathScore = geometryScore × extinctionFactor × 100
```

**气溶胶散射修正（需求43）**：
```
// AOD 表示气溶胶光学厚度，影响红橙色散射潜力；PM/Dust 用于灰霾/沙尘风险
if (AOD is missing) aerosolFactor = 1.0
else if (AOD < 0.08) aerosolFactor = 0.98        // 空气过净，颜色可能偏淡
else if (0.08 <= AOD <= 0.35) aerosolFactor = 1.03~1.08  // 适中，增强红橙散射
else aerosolFactor = 0.85~0.95                  // 过高，灰霾/沙尘压低观感

if (visibility < 8km && (AOD high || PM high || dust high)) aerosolFactor = min(aerosolFactor, 0.85)
```

**最终融合**：
```
FINAL_WEIGHTS = { CLOUD_CANVAS: 0.8, LIGHT_PATH: 0.2 }
finalScore = canvasScore × 0.8 + lightPathScore × 0.2
finalScore = finalScore × aerosolFactor  // 仅在有气溶胶数据时启用；默认 1.0
```

### 质量等级
- **优秀**：≥70分，绿色
- **良好**：40-69分，黄色/橙色
- **一般**：<40分，灰色

---

## 变更摘要

### 2026-05-31
- **需求54**：新增统一用户账号与第三方登录。网页端支持微信开放平台网站应用扫码登录与 Google OAuth/OIDC，小程序端支持微信登录；通过 `users + user_identities + sessions` 和微信 `unionid` 打通两端，登录后照片上传、留言反馈、API 申请统一归属 `userId`。平台申请包含微信开放平台账号/网站应用、小程序 AppSecret、Google OAuth Client、生产 `AUTH_SECRET`、隐私政策和用户协议。
- **需求55**：新增后台访客与运营分析。后台从访客计数升级为运营分析面板，覆盖 UV/PV、来源渠道、热门查询、功能点击、转化漏斗、照片/反馈/API 申请、接口失败率、慢请求和小程序错误；采集只保留 hash 与粗粒度地区，不保存明文 IP、token、openid/unionid 或精确个人轨迹。

### 2026-05-26
- **需求53**：新增 GFS+CAMS 火烧云地图数据管线与后台控制台。地图链路从 Open-Meteo 点位扫网格升级为 GFS 天气网格 + CAMS 气溶胶网格批处理；后台支持数据源模式切换、拉取范围管理、资源预估、任务进度、下载量追溯、清理和回滚；默认按腾讯云 `SA2.LARGE4`（4 核、3.6GiB RAM、2GiB swap、40G 系统盘、约 18G 可用）约束设计未来 48 小时地图。

### 2026-05-27
- **需求53.16-53.18 运维与验收材料**：补充 GFS+CAMS 地图管线的运维口径。后台必须能从 `/admin`、`/api/admin/data-pipeline/status`、`/api/admin/data-pipeline/runs` 和地图接口 metadata 判断当前模式、当前 run/step、今日 GFS/CAMS 下载量、cleanup 释放量，以及地图使用的 `source/cycle/bbox/resolution/sourceMeta`。小资源验收必须先在腾讯云 `SA2.LARGE4` 上用测试小区域跑通，再扩大到中国范围；验收时必须记录峰值内存、swap、raw/tmp 峰值、下载量、耗时和地图溯源。

### 2026-05-28
- **需求53.19 统一缓存管理系统**：后台数据管线面板必须整合新 GFS/CAMS 产品缓存与旧 Open-Meteo Grid 缓存，统一展示公开地图当前 source/mode/status、GFS/CAMS 产品数量与占用、Open-Meteo sunrise/sunset 拉取/插值进度、缓存可用性和错误原因；同一面板支持 Open-Meteo legacy 刷新与 `hybrid/gfs_cams/openmeteo/cache_only/paused` 模式切换。

### 2026-05-09
- **需求51**：照片分享元数据增强与地图聚合展示。上传照片新增上传者、地点、拍摄时间、上传时间等字段；上传者可缺省为“网友”；地点可由经纬度反向地理编码自动生成，也可手动输入且不影响经纬度；拍摄时间优先从 EXIF 自动读取，也可手动输入且不影响坐标；上传时间由服务端按北京时间记录与展示；后台照片管理支持编辑上传者、地点、拍摄时间、描述和经纬度，其中上传时间只读不可改；同一地点多张照片参考 Apple 相册地图做聚合/堆叠展示。

### 2026-05-11
- **需求52**：确定微信小程序为霞客下一阶段主产品入口，选择原生小程序路线；现有后端 API 继续复用，未来 iOS 复用同一 API 契约与设计语言。小程序代码放当前仓库 `miniprogram/`，不单独建库。

### 2026-05-07
- **需求48**：预测解释与图表可读性优化（PR #589 分数明细移动端 ledger、PR #590 24 小时温度图附天气标签）。
- **文档优化**：补齐需求47/48，使 requirements/design/tasks 三文档与近期已合并实现同步。

### 2026-04-25
- **需求46**：朝霞/晚霞预测卡片概念界面重构（借鉴概念图信息结构，保留霞客现有视觉体系，云况雷达不变）
- **需求45**：Agent API 与 API Token 管理（大模型/脚本调用，后台发放 Token，独立限流与审计）

### 2026-03-29
- **需求41**：API调用日志记录（分类：grid/weather/gaode/gaode_tile，管理后台展示）
- **需求42**：定时更新配置（可设置更新时间、朝霞/晚霞选择，持久化配置）
- **需求40**：Open-Meteo配额统计与额度保护（软上限9000/10000）
- **需求39**：网格抓取队列系统（批次大小10，间隔2500ms，429熔断60s）
- 分享地图页底图改为中国GeoJSON自研地图（PR #318）
- 管理后台新增配额面板、队列状态面板（PR #319）
- 手机版卡片布局优化：单列长条+横排信息
- 暗色模式云图颜色调亮、去除contrast滤镜

### 2026-03-17
- **需求37**：朝霞/晚霞评分散点地图（中国区域，评分≥60标注）
- **需求38**：火烧云全球分享地图（照片上传、访客记录）

### 2026-03
- **需求35**：光路评分物理重构（太阳几何关系、雨雪封顶）
- **需求36**：Windy彻底移除（预测链路完全移除Windy依赖）

### 2026-02
- **需求27-29**：品牌升级、设置重组、访客持久化
- **需求31-34**：Open-Meteo迁移、Windy下线

### 2026-01及更早
- **需求1-26**：核心功能、增强功能、架构重构、多语言、测试覆盖
