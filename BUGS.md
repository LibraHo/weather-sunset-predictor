# 已知Bug列表

## Bug 1: 非北京城市只显示晚霞
**描述**: 只有北京的朝霞晚霞模块显示正确，其他城市（如深圳、成都）只显示晚霞，缺少朝霞

**复现步骤**:
1. 搜索深圳或成都
2. 查看晚霞预测模块
3. 只看到晚霞预测，没有朝霞预测

**原因**: 日出日落时间计算时未处理跨日情况（分钟数为负或超过24小时）

**修复**: 已修复 `src/services/SunsetPredictionService.js` 中的 `getSunriseTime` 和 `getSunsetTime` 方法

**状态**: ✅ 已修复

---

## Bug 2: 7天概览实际只显示4天
**描述**: 标题写"7天概览"，但实际只显示4天的天气数据

**原因**: Windy API 请求缺少 hours 参数，只返回默认的80小时数据

**修复**:
1. 已修复 `src/services/WindyAPIService.js`，在请求体中添加 hours 参数
2. 已修复 `src/controllers/WeatherController.js`，标题根据实际天数显示

**状态**: ✅ 已修复

---

## Bug 3: UI显示混乱
**描述**: 7天概览中，4天天气卡片下面显示的是当前的湿度、气压等信息，而不是对应日期的信息

**原因**: 当前时刻天气显示区域位于概览卡片下方，造成用户混淆

**修复**: 已重新组织 `index.html` 中的布局，将当前时刻天气移到视图切换按钮之前

**状态**: ✅ 已修复

---

## Bug 4: API密钥不保存
**描述**: 每次打开应用都需要重新输入API密钥，密钥没有保存到localStorage

**说明**: API密钥保存功能已经实现（`StorageService.saveAPIKey`）。可能是使用模拟API时不需要真实密钥，或者浏览器设置问题。

**需要用户确认**:
- 是否使用真实Windy API？
- 浏览器是否允许localStorage？

**状态**: ⏸️ 待确认

---

## Bug 5: 天气信息缺少城市名显示
**描述**: 天气信息区域没有显示当前查询的城市名称

**修复**:
1. 修改HTML添加`location-name`元素
2. 修改`WeatherController.updateWeatherDisplay`方法接受location参数
3. 修改`AppController.handleLocationChange`传递location信息

**状态**: ✅ 已修复

---

## Bug 6: 7天概览实际只显示4天（续）
**说明**: 这是Windy Point Forecast API的限制，该API不接受自定义小时数参数，固定返回约80小时数据

**解决方案**:
1. 移除无效的hours参数
2. 动态更新概览标题显示实际天数（如"4天概览"）
3. 添加信息提示说明数据源限制

**如果需要完整7天数据**:
- 使用MockWindyAPIService进行测试（在app.js中设置`USE_MOCK_API = true`）
- 或考虑更换其他天气数据源（如OpenWeatherMap等）

**状态**: ✅ 已优化（添加提示说明）

---

---

## Bug 7: 朝霞/晚霞预测只显示一种类型
**描述**: 某些城市（如深圳、圣地亚哥）只显示朝霞预测，某些城市（如成都、拉萨）只显示晚霞预测，没有解释原因

**原因**: Windy API天气数据从当前时刻开始。如果查询时间为17:00，今日的日出时间（07:05）已过，没有对应的天气数据，无法生成预测。反之，如果查询时间较早，日落数据可能尚未到达。

**修复**:
1. 修改 `src/controllers/PredictionController.js` 的 `updateTodayPredictions` 方法
2. 当朝霞或晚霞预测不可用时，显示友好的提示卡片
3. 添加CSS样式 `styles/main.css` 支持不可用提示卡片
4. 根据实际日出日落时间判断"时间已过"（日出+2小时、日落+1.5小时）

**状态**: ✅ 已修复

---

## Bug 8: 将明天的朝霞/晚霞误显示为今天的
**描述**: 当今天（第0天）的朝霞或晚霞预测因数据不足无法生成时，系统会错误地将明天（第1天）的预测显示为"今日预测"

**原因**: `updatePredictionDisplay` 方法中使用 `predictions.find(p => p.type === 'sunrise')` 只查找第一个朝霞/晚霞预测，没有验证日期是否为今天

**修复**:
1. 修改 `updatePredictionDisplay` 方法，同时检查 `p.type` 和 `p.date`
2. 只有 `p.date.toDateString() === today.toDateString()` 时才认为是今日预测
3. 添加更详细的日志输出，显示每个预测的日期

**状态**: ✅ 已修复

---

## 修复记录
| 日期 | Bug编号 | 修复者 | 说明 |
|------|---------|--------|------|
| 2026-01-24 | 1 | Claude | 修复日出日落时间计算的跨日问题 |
| 2026-01-24 | 2 | Claude | 移除无效hours参数，动态更新概览标题，添加提示 |
| 2026-01-24 | 3 | Claude | 重新组织HTML布局，避免UI混淆 |
| 2026-01-24 | 4 | - | 待用户确认是否真实存在问题 |
| 2026-01-24 | 5 | Claude | 添加城市名显示到天气信息区域 |
| 2026-01-24 | 6 | Claude | 说明Windy API限制，添加提示信息 |
| 2026-01-24 | 7 | Claude | 添加朝霞/晚霞预测不可用时的友好提示，根据实际日出日落时间判断"时间已过" |
| 2026-01-24 | 8 | Claude | 修复将明天的预测误显示为今天的问题，增加日期验证 |
