.app-topbar-shell {
  position: relative;
  z-index: 20;
  flex: none;
}

.home-topbar {
  position: relative;
  z-index: 3;
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 132rpx;
  box-sizing: border-box;
  padding: 28rpx 30rpx;
  border: 2rpx solid rgba(127, 83, 29, 0.15);
  border-radius: 24rpx;
  background: rgba(255, 252, 246, 0.90);
  box-shadow: 0 28rpx 76rpx rgba(92, 58, 20, 0.10), inset 0 1rpx 0 rgba(255, 255, 255, 0.90);
}

.home-brand-row {
  display: flex;
  align-items: center;
  gap: 22rpx;
}

.app-logo-image {
  width: 74rpx;
  height: 74rpx;
  display: block;
}

.app-logo-text {
  color: #7a4a18;
  font-family: "Cormorant Garamond", Georgia, "Songti SC", "STSong", serif;
  font-size: 42rpx;
  font-weight: 500;
  letter-spacing: 0;
  line-height: 1;
}

.home-header-actions {
  display: flex;
  flex: none;
  align-items: center;
  gap: 12rpx;
}

.home-view-menu {
  position: relative;
  flex: none;
}

.icon-button,
.menu-button,
.header-icon-button {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0;
  border: 0;
  border-radius: 20rpx;
  color: #6a421d;
  line-height: 1;
  background: rgba(255, 255, 255, 0.86);
  box-shadow: 0 12rpx 26rpx rgba(92, 58, 20, 0.10);
}

.menu-button,
.header-icon-button {
  width: 70rpx;
  min-width: 70rpx;
  max-width: 70rpx;
  height: 70rpx;
  box-sizing: border-box;
}

.header-action-image {
  width: 42rpx;
  height: 42rpx;
  display: block;
}

.home-view-menu-dropdown {
  position: absolute;
  top: 82rpx;
  right: 0;
  width: 286rpx;
  box-sizing: border-box;
  padding: 10rpx;
  border: 1rpx solid rgba(127, 83, 29, 0.14);
  border-radius: 16rpx;
  background: rgba(255, 252, 246, 0.98);
  box-shadow: 0 18rpx 46rpx rgba(92, 58, 20, 0.16);
}

.home-view-option {
  min-height: 56rpx;
  box-sizing: border-box;
  padding: 12rpx 16rpx;
  border-radius: 12rpx;
  color: #6f5d51;
  font-size: 25rpx;
  font-weight: 800;
  line-height: 32rpx;
}

.home-view-option.active {
  color: #ffffff;
  background: #39a849;
}

.settings-panel {
  margin-top: 18rpx;
  padding: 24rpx;
  color: #3d3028;
  border: 2rpx solid rgba(127, 83, 29, 0.15);
  border-radius: 24rpx;
  background: rgba(255, 252, 246, 0.90);
  box-shadow: 0 28rpx 76rpx rgba(92, 58, 20, 0.10), inset 0 1rpx 0 rgba(255, 255, 255, 0.90);
}

.settings-panel-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18rpx;
}

.settings-title {
  color: #3d3028;
  font-size: 32rpx;
  font-weight: 900;
}

.settings-subtitle {
  margin-top: 6rpx;
  color: rgba(89, 67, 54, 0.66);
  font-size: 23rpx;
  line-height: 1.45;
}

.settings-close {
  width: 52rpx;
  height: 52rpx;
  margin: 0;
  padding: 0;
  border-radius: 999rpx;
  color: rgba(89, 67, 54, 0.72);
  font-size: 34rpx;
  line-height: 46rpx;
  background: rgba(255, 255, 255, 0.08);
}

.settings-group {
  margin-top: 22rpx;
}

.settings-label {
  color: #7a4a18;
  font-size: 24rpx;
  font-weight: 800;
}

.settings-segmented {
  display: flex;
  gap: 10rpx;
  margin-top: 12rpx;
  padding: 7rpx;
  border-radius: 14rpx;
  background: rgba(127, 83, 29, 0.08);
}

.settings-segment {
  flex: 1;
  height: 62rpx;
  border-radius: 10rpx;
  color: rgba(89, 67, 54, 0.62);
  font-size: 25rpx;
  font-weight: 800;
  line-height: 62rpx;
  text-align: center;
}

.settings-segment.active {
  color: #ffffff;
  background: #39a849;
}

.settings-done {
  width: 100%;
  height: 72rpx;
  margin-top: 26rpx;
  border-radius: 14rpx;
  color: #1b1206;
  font-size: 28rpx;
  font-weight: 900;
  line-height: 72rpx;
  background: linear-gradient(135deg, #ffd166, #fb923c);
}

button::after {
  border: 0;
}
